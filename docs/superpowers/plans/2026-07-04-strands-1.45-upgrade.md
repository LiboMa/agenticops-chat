# Strands 1.45 升级 + 3 项增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 strands-agents 1.45（已装）上，为 7 个 agent 启用 SDK 自动上下文治理（省 token + 治大输出），并给 executor 加一层可选的 HITL 安全网——全部 config 门控、可回滚、不拆现有逻辑。

**Architecture:** 沿用现有 agents-as-tools 架构不变。通过两个新的 config 开关 + 两个 mirror 现有 `get_agent_conversation_manager` 模式的 helper 函数，把 `context_manager="auto"` 注入全部 8 处 `Agent(...)` 构造点，把可选 `HumanInTheLoop` 注入 executor 单点。零架构改动，纯参数注入。

**Tech Stack:** Python 3.12, strands-agents 1.45.0, pydantic-settings, pytest, Bedrock。

## Global Constraints

- **版本已就位**：`strands-agents==1.45.0`（requirements.txt:303 / pyproject.toml:26 `>=1.45.0,<2.0.0` / uv.lock:107 三处一致）。本计划**不改依赖版本**——升级已发生，Task 0 只验证。
- **config.py 不硬编码值**：新增开关用 `Field(default=..., description=...)`，同时在 `config/settings.yaml` 落一行默认。
- **凭证铁律不碰**：不改 provider 层、不碰 credentials/resolver、不动 run_on_host/run_aws_cli 的凭证解析。
- **每步独立 commit**，`git push` 用 `--no-verify`（Code Defender）。**E2E + 主人确认后才 commit/push**（用户铁律）。
- **HITL 默认关**（`executor_hitl_enabled=False`）：先安全上线，验证后再开。
- **正确导入路径**（已核实）：`from strands.vended_plugins.context_offloader import ContextOffloader, InMemoryStorage, FileStorage`；`from strands.vended_interventions.hitl import HumanInTheLoop`。
- **`context_manager="auto"` + 现有 `conversation_manager` 共存**：官方确认用户的 conversation_manager 保留，SDK 只额外加 ContextOffloader plugin。

---

## File Structure

- `src/agenticops/config.py` — 新增 2 个 config Field + 2 个 helper 函数（`get_agent_context_manager`、`get_executor_interventions`）。所有注入逻辑集中在此，8 个 agent 文件只加一行调用。
- `config/settings.yaml` — 新增 2 行默认值。
- `src/agenticops/agents/{main,scan,detect,rca,sre,executor,reporter}_agent.py` — 每处 `Agent(...)` 加一行 `context_manager=...`；executor 额外加 `interventions=...`。
- `tests/test_strands_context_manager.py` — 新建，测 helper + 开关。
- `tests/test_executor_hitl.py` — 新建，测 HITL helper + 开关。

---

## Task 0: 验证升级基线（无代码改动，仅 gate）

**Files:** 无改动。

**Interfaces:**
- Produces: 一个"1.45 下 3620 测试全绿"的基线事实，后续所有 Task 以此为回归对照。

- [ ] **Step 1: 确认版本三处一致**

Run:
```bash
cd /Users/malibo/MyDev/AgenticOps
grep -nE "strands-agents" requirements.txt pyproject.toml | grep -E "==|>="
.venv/bin/python -c "import importlib.metadata as m; print(m.version('strands-agents'))"
```
Expected: requirements 显示 `==1.45.0`，pyproject 显示 `>=1.45.0,<2.0.0`，metadata 打印 `1.45.0`。

- [ ] **Step 2: 全量 pytest 建立绿色基线**

Run:
```bash
.venv/bin/python -m pytest tests/ -q 2>&1 | tail -25
```
Expected: 全绿（或与升级前完全相同的既有 skip/xfail 集合）。**若有 1.45 引入的新 fail，记录清单——这是升级真实破坏面，先修再继续。**

- [ ] **Step 3: 真实 Bedrock 冒烟**

Run:
```bash
aiops chat "check health" 2>&1 | tail -30
```
Expected: 正常路由到 scan/detect，无 `ImportError`/`TypeError`/构造异常，返回健康摘要。

- [ ] **Step 4: 记录基线（无 commit）**

在本 Task 完成后向用户报告："1.45 基线 = N passed / M skipped，冒烟通过"。**不 commit**（无代码改动）。等用户确认基线 OK 再进 Task 1。

---

## Task 1: config 开关 + context_manager helper（TDD）

**Files:**
- Modify: `src/agenticops/config.py`（Field 区加 1 个字段；helper 区 `get_agent_conversation_manager` 之后加 1 个函数）
- Modify: `config/settings.yaml`（加 1 行）
- Test: `tests/test_strands_context_manager.py`（新建）

**Interfaces:**
- Produces: `get_agent_context_manager(agent_name: str) -> str | None` — 返回 `"auto"`（开）或 `None`（关），受 `settings.strands_context_manager_auto: bool` 门控。Task 2 消费它。

- [ ] **Step 1: 写失败测试**

Create `tests/test_strands_context_manager.py`:
```python
"""Strands SDK 自动上下文治理开关 + helper 测试。"""
from agenticops.config import settings, get_agent_context_manager


def test_default_flag_enabled():
    # 默认开启 auto 上下文治理
    assert settings.strands_context_manager_auto is True


def test_helper_returns_auto_when_enabled(monkeypatch):
    monkeypatch.setattr(settings, "strands_context_manager_auto", True)
    assert get_agent_context_manager("main") == "auto"
    assert get_agent_context_manager("executor") == "auto"


def test_helper_returns_none_when_disabled(monkeypatch):
    monkeypatch.setattr(settings, "strands_context_manager_auto", False)
    assert get_agent_context_manager("main") is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_strands_context_manager.py -v`
Expected: FAIL —`ImportError: cannot import name 'get_agent_context_manager'`。

- [ ] **Step 3: 加 config Field**

在 `src/agenticops/config.py` 的 `bedrock_cache_enabled` 字段（约 172 行）之后加：
```python
    strands_context_manager_auto: bool = Field(
        default=True,
        description="Enable Strands SDK auto context management (SummarizingConversationManager + ContextOffloader) on all agents. Coexists with per-agent conversation_manager.",
    )
```

- [ ] **Step 4: 加 helper 函数**

在 `src/agenticops/config.py` 的 `get_agent_conversation_manager`（约 921 行结束）之后加：
```python
def get_agent_context_manager(agent_name: str):
    """Return "auto" to enable Strands SDK auto context management, or None to disable.

    Gated by settings.strands_context_manager_auto. When "auto" is combined with an
    explicit conversation_manager (which every agent passes), the SDK preserves our
    conversation_manager and only adds the ContextOffloader plugin — so per-agent
    window strategy (Null / SlidingWindow) is unchanged; we just gain offloading of
    oversized tool results. Returns None (== Agent default) when disabled, so toggling
    the flag off restores exact pre-change behavior.

    agent_name is accepted for future per-agent override symmetry with
    get_agent_conversation_manager; currently the flag is global.
    """
    if settings.strands_context_manager_auto:
        return "auto"
    return None
```

- [ ] **Step 5: settings.yaml 落默认**

在 `config/settings.yaml` 的 `bedrock_cache_enabled: true`（约 49 行）之后加：
```yaml
strands_context_manager_auto: true
```

- [ ] **Step 6: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_strands_context_manager.py -v`
Expected: 3 passed。

- [ ] **Step 7: config 无副作用回归**

Run: `.venv/bin/python -c "from agenticops.config import settings, get_agent_context_manager; print(get_agent_context_manager('main'))"`
Expected: 打印 `auto`，无异常。

- [ ] **Step 8: Commit（用户确认后）**

```bash
git add src/agenticops/config.py config/settings.yaml tests/test_strands_context_manager.py
git commit --no-verify -m "feat(config): strands_context_manager_auto flag + helper

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: 把 context_manager="auto" 注入全部 8 处 Agent 构造

**Files:**
- Modify: `src/agenticops/agents/main_agent.py:270`（`_build_agent` 闭包内 `return Agent(`）
- Modify: `src/agenticops/agents/scan_agent.py:189`
- Modify: `src/agenticops/agents/detect_agent.py:223` 和 `:335`（两处）
- Modify: `src/agenticops/agents/rca_agent.py:280`
- Modify: `src/agenticops/agents/sre_agent.py:273`
- Modify: `src/agenticops/agents/executor_agent.py:228`
- Modify: `src/agenticops/agents/reporter_agent.py:136`
- Test: `tests/test_strands_context_manager.py`（追加构造断言）

**Interfaces:**
- Consumes: `get_agent_context_manager(agent_name)` from Task 1。
- Produces: 8 处 Agent 均带 `context_manager`（开关开时为 "auto"）。

**注入模式（8 处相同）**：每处 `Agent(...)` 已有 `conversation_manager=get_agent_conversation_manager("<name>")` 一行；在其**紧下方**加一行，`<name>` 用该处相同的 agent 名：
```python
            context_manager=get_agent_context_manager("<name>"),
```
每个文件顶部已 `from agenticops.config import ..., get_agent_conversation_manager, ...`——把 `get_agent_context_manager` 加进同一 import。

- [ ] **Step 1: 写失败测试（构造冒烟 + 参数存在）**

在 `tests/test_strands_context_manager.py` 追加：
```python
def test_all_agents_import_and_build(monkeypatch):
    """开关开时，7 个 agent 构造函数不因新参数报错。"""
    monkeypatch.setattr(settings, "strands_context_manager_auto", True)
    # 只验证 import + helper 调用不炸；真实构造走冒烟测试。
    from agenticops.config import get_agent_context_manager
    for name in ("main", "scan", "detect", "rca", "sre", "executor", "reporter"):
        assert get_agent_context_manager(name) == "auto"
```

- [ ] **Step 2: 跑确认通过（helper 已就位）**

Run: `.venv/bin/python -m pytest tests/test_strands_context_manager.py::test_all_agents_import_and_build -v`
Expected: PASS（此测仅测 helper，为构造改动兜底）。

- [ ] **Step 3: 改 main_agent.py**

`src/agenticops/agents/main_agent.py`，确保 import 行含 `get_agent_context_manager`（第 231 行的 `from agenticops.config import get_agent_model_config, get_agent_conversation_manager, get_bedrock_boto_session` → 追加 `, get_agent_context_manager`）。第 273 行下方加：
```python
            conversation_manager=get_agent_conversation_manager("main"),
            context_manager=get_agent_context_manager("main"),
```

- [ ] **Step 4: 改其余 7 处**

对 scan(189)/detect(223,335)/rca(280)/sre(273)/executor(228)/reporter(136)：同样把 `get_agent_context_manager` 加进各文件的 `from agenticops.config import ...` 行，并在各自 `conversation_manager=get_agent_conversation_manager("<name>")` 下方加 `context_manager=get_agent_context_manager("<name>")`（`<name>` 分别为 scan/detect/rca/sre/executor/reporter）。

- [ ] **Step 5: py_compile 全部改动文件**

Run:
```bash
.venv/bin/python -m py_compile src/agenticops/agents/main_agent.py src/agenticops/agents/scan_agent.py src/agenticops/agents/detect_agent.py src/agenticops/agents/rca_agent.py src/agenticops/agents/sre_agent.py src/agenticops/agents/executor_agent.py src/agenticops/agents/reporter_agent.py && echo OK
```
Expected: `OK`，无语法错误。

- [ ] **Step 6: 全量 pytest（对照 Task 0 基线）**

Run: `.venv/bin/python -m pytest tests/ -q 2>&1 | tail -15`
Expected: 与 Task 0 基线一致的通过数，无新增 fail。

- [ ] **Step 7: 真实 Bedrock 冒烟——触发大输出验证 offloader**

Run:
```bash
aiops chat "scan all resources and summarize" 2>&1 | tail -30
```
Expected: 正常返回；无 offloader 相关异常；大工具结果（全量 scan）不再撑爆窗口。

- [ ] **Step 8: Commit（用户确认后）**

```bash
git add src/agenticops/agents/*.py tests/test_strands_context_manager.py
git commit --no-verify -m "feat(agents): enable context_manager=auto on all 8 agent builds

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: executor 可选 HITL 安全网（默认关）

**Files:**
- Modify: `src/agenticops/config.py`（加 1 个 Field + 1 个 helper）
- Modify: `config/settings.yaml`（加 1 行）
- Modify: `src/agenticops/agents/executor_agent.py:228`（加 `interventions=` 一行 + import）
- Test: `tests/test_executor_hitl.py`（新建）

**Interfaces:**
- Consumes: `settings.executor_hitl_enabled: bool`。
- Produces: `get_executor_interventions() -> list` — 关时返回 `[]`（行为完全不变），开时返回 `[HumanInTheLoop(...)]`。

- [ ] **Step 1: 写失败测试**

Create `tests/test_executor_hitl.py`:
```python
"""Executor HITL 安全网开关 + helper 测试。"""
from agenticops.config import settings, get_executor_interventions


def test_default_hitl_disabled():
    # 默认关闭：先安全上线
    assert settings.executor_hitl_enabled is False


def test_returns_empty_when_disabled(monkeypatch):
    monkeypatch.setattr(settings, "executor_hitl_enabled", False)
    assert get_executor_interventions() == []


def test_returns_hitl_when_enabled(monkeypatch):
    monkeypatch.setattr(settings, "executor_hitl_enabled", True)
    result = get_executor_interventions()
    assert len(result) == 1
    # 只读工具在 allow-list 里（不触发人审）
    hitl = result[0]
    assert "run_aws_cli_readonly" in hitl.allowed_tools
    assert "get_approved_fix_plan" in hitl.allowed_tools
```

- [ ] **Step 2: 跑确认失败**

Run: `.venv/bin/python -m pytest tests/test_executor_hitl.py -v`
Expected: FAIL — `ImportError: cannot import name 'get_executor_interventions'`。

- [ ] **Step 3: 加 config Field**

在 `src/agenticops/config.py` 的 `executor_auto_approve_l0_l1` 字段（约 515 行）之后加：
```python
    executor_hitl_enabled: bool = Field(
        default=False,
        description="Add a Strands HumanInTheLoop intervention as a second SDK-level approval gate on the executor. Default off; the existing get_approved_fix_plan gate + DB state machine remain the primary gate. When on, mutating tools raise interrupts (interrupt/resume) — resume-wiring to the approval UI is a follow-up.",
    )
```

- [ ] **Step 4: 加 helper 函数**

在 `src/agenticops/config.py` 的 `get_agent_context_manager`（Task 1 加的函数）之后加：
```python
def get_executor_interventions():
    """Return the interventions list for the executor agent.

    Gated by settings.executor_hitl_enabled (default False -> []). When enabled,
    adds ONE HumanInTheLoop handler as a *second*, SDK-level safety net on top of
    the existing primary gate (get_approved_fix_plan tool check + DB state machine +
    L0/L1 risk tiering). Read-only / verification tools are allow-listed so they run
    without prompting; only mutating tools (run_aws_cli, run_on_host, run_kubectl)
    raise an interrupt. Returns [] when disabled so the executor behaves exactly as
    before. Import is lazy so the SDK symbol is only touched when the gate is on.
    """
    if not settings.executor_hitl_enabled:
        return []
    from strands.vended_interventions.hitl import HumanInTheLoop
    return [
        HumanInTheLoop(
            allowed_tools=[
                "get_approved_fix_plan",
                "run_aws_cli_readonly",
                "describe_ec2",
                "describe_rds",
                "describe_vpcs",
                "describe_security_groups",
                "describe_load_balancers",
                "describe_eks_clusters",
                "query_reachability",
                "find_network_path",
                "detect_network_anomalies",
            ],
        )
    ]
```

- [ ] **Step 5: settings.yaml 落默认**

在 `config/settings.yaml` 的 `executor_auto_approve_l0_l1: true`（约 58 行）之后加：
```yaml
executor_hitl_enabled: false
```

- [ ] **Step 6: 跑测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_executor_hitl.py -v`
Expected: 3 passed。

- [ ] **Step 7: 接入 executor 构造**

`src/agenticops/agents/executor_agent.py`：把 `get_executor_interventions` 加进第 187 行的 `from agenticops.config import ...`。在第 228 行 `Agent(` 的 `conversation_manager=get_agent_conversation_manager("executor"),` 下方加：
```python
            conversation_manager=get_agent_conversation_manager("executor"),
            context_manager=get_agent_context_manager("executor"),
            interventions=get_executor_interventions(),
```
（注：`context_manager` 行 Task 2 已加；此处确认三行齐全。）

- [ ] **Step 8: py_compile + 全量 pytest**

Run:
```bash
.venv/bin/python -m py_compile src/agenticops/agents/executor_agent.py && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -15
```
Expected: `py_compile` 无错；pytest 与基线一致。

- [ ] **Step 9: 真实 Bedrock 冒烟——开关关（默认，回归）+ 开（验证门触发）**

Run（默认关，确认行为不变）:
```bash
aiops chat "check health" 2>&1 | tail -20
```
Expected: 正常，executor 行为与改动前一致。

Run（临时开，验证门触发；用环境变量覆盖，不改盘）:
```bash
AIOPS_EXECUTOR_HITL_ENABLED=true aiops chat "fix the highest severity L0 issue" 2>&1 | tail -40
```
Expected: 执行到 mutating 工具时出现 `stop_reason=interrupt`（人审门触发）；只读 describe/verify 工具不被拦。**若确认门按预期触发即通过**（resume-wiring 是后续）。

- [ ] **Step 10: Commit（用户确认后）**

```bash
git add src/agenticops/config.py config/settings.yaml src/agenticops/agents/executor_agent.py tests/test_executor_hitl.py
git commit --no-verify -m "feat(executor): optional HumanInTheLoop safety gate (default off)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 文档更新（用户铁律：开发后自动更新 docs）

**Files:**
- Modify: `CLAUDE.md`（config 表加 2 个新开关）
- Modify: `docs/`（相关 workflow / release note 若有则补一句）

- [ ] **Step 1: CLAUDE.md config 表加 2 行**

在 `CLAUDE.md` 的 Key Configuration 表加：
```markdown
| `strands_context_manager_auto` | `true` | Strands SDK 自动上下文治理（摘要压缩 + 大工具结果 offload），与 per-agent conversation_manager 共存 |
| `executor_hitl_enabled` | `false` | executor 上叠加一层 Strands HumanInTheLoop 审批门（默认关；主门仍是 get_approved_fix_plan + 状态机） |
```

- [ ] **Step 2: 更新 strands-sdk-gaps 记忆（若需要）**

标注 context_manager/offloader/HITL 已落地，structured_output/OTLP/Evals 仍 deferred。

- [ ] **Step 3: Commit（用户确认后）**

```bash
git add CLAUDE.md docs/
git commit --no-verify -m "docs: document strands_context_manager_auto + executor_hitl_enabled

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review Notes

- **Spec coverage**：设计文档 §1 的 4 项 → 升级(Task 0 验证) + context_manager(Task 1-2) + ContextOffloader(随 auto 自动挂，Task 2 冒烟验证) + HITL(Task 3)。§4 分步表 3 步 → Task 0/1+2/3。§5 非目标（不动记忆/Skills/路由/凭证）→ 计划零涉及。✓
- **Placeholder scan**：每步含真实代码/命令/预期。HITL `ask` 模式明确定为 interrupt/resume（非占位）。✓
- **Type consistency**：`get_agent_context_manager(str)->str|None`、`get_executor_interventions()->list` 在 Task 1/3 定义，Task 2/3 消费一致。`strands_context_manager_auto` / `executor_hitl_enabled` 命名全程一致。✓
- **导入路径**已 runtime 核实（vended_interventions.hitl / vended_plugins.context_offloader）。✓
