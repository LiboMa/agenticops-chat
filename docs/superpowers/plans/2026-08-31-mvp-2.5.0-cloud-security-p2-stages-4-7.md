# MVP-2.5.0 云安全审查 P2 · 阶段 4-7 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在已交付的 P1 核心（Stage 0-3：态势采集 + CIS 评分 + 含 NACL 三态可达性）之上，交付快频增量采集 + signal_gate 接入 + 证据接地建议器 + Security 页/API/`security-review` 报告 + 真实账户 E2E——完成 spec §9 的阶段 4-7。

**Architecture:** 快频 `run_incremental_poll` 逐账户×区域×源（GuardDuty/SecurityHub/CloudTrail 高危事件）按 cursor 增量拉官方 findings，经 `signal_gate.process_signal` 折叠成 HealthIssue；慢频 `run_posture_snapshot` 补齐 exposure_paths 接线（聚合拓扑 → `reachability.annotate`）并把可执行发现入 gate；advisor 是唯一 LLM 环节（证据接地 + critic，fail-closed）；Web 层镜像 cost router / Galaxy hooks / AgentMetrics recharts 模式。

**Tech Stack:** Python 3.12 · SQLAlchemy 2.0 · FastAPI · boto3（经 provider 层）· pytest（AWS/LLM 全 mock）· React + TanStack Query + recharts 3。

**Spec:** `docs/superpowers/specs/2026-08-29-cloud-security-review-p1-design.md`（§2 数据流、§3.5-3.8 组件、§6 失败模式、§7 测试计划）。前序计划：`docs/superpowers/plans/2026-08-30-mvp-2.5.0-cloud-security-p1-core.md`（Stage 0-3 已全部落地提交）。

**本计划的 4 个范围决策**（偏离 spec 处均有理由，执行者不要"纠正"回去）：
1. **快频源取 GuardDuty + SecurityHub + CloudTrail 高危事件三个**；AWS Config 不单独拉——SecurityHub 已聚合 Config findings，且 poller 架构逐源独立 fail-soft，后续加源零改动编排（简约/经济原则）。
2. **快频可达性富化 = 查最新快照的 exposure_paths**（确定性查表），不在 10 分钟路径上做实时拓扑聚合（经济原则；慢频每小时已刷新 exposure_paths）。
3. **慢频只把"可执行发现"入 gate**：`severity_hint == "high"` 或 `reachability == "reachable"` 的 finding 才产 Signal（spec §2 的"可执行发现"落地定义，防 issue 洪泛）；medium 结构项留在快照与 Security 页展示。
4. **Dashboard 高亮卡从阶段 4 挪到阶段 6**：它依赖 `/api/security/summary`（阶段 6 交付物）；阶段 4 的可验证产出仍是"新 finding 分钟级进 issue 列表"（signal_gate → HealthIssue → 既有 issues 列表自动可见）。

## Global Constraints

以下为项目级硬约束，每个 Task 的要求都隐含包含本节（值逐字取自 spec 与 CLAUDE.md）：

- **凭证铁律**：任何跨账户业务调用必须经 provider 层取**目标账户**凭证；所有 AWS 采集统一经 `agenticops.security.collectors._get_client(service, region, account)`（内部走 `aws_tools._get_client`），**绝不裸用 `boto3.client()`**，账户解析失败=显式报错跳过该账户，**绝不回退 ambient**。
- **config.py 不硬编码**：本计划不新增 config 项（8 个 `security_*` 已在 Stage 0 落地：`security_review_enabled/security_poll_interval_minutes/security_posture_interval_minutes/security_reachability_nacl_enabled/security_advisor_enabled/security_advisor_critic_enabled/security_snapshot_retention_days/security_model_id`）。若执行中发现确需新配置，先停下询问主人。
- **保守偏报（原则 3）**：可达性任一输入数据缺失 → `undetermined`，绝不降为 `not_reachable`。
- **评分客观可复现（原则 4）**：评分无随机、无时间依赖、无 LLM；可达性与建议**绝不篡改分数**。
- **fail-soft 采集 / fail-closed 建议**：单采集源异常 → WARNING + 跳过 + cursor 不前移；建议器任何异常/不接地/refuted → 丢弃不入库。
- **LLM 纪律**：仅 advisor 触碰 LLM（`signal_gate._call_bedrock`，temp=0，模型 `settings.security_model_id or settings.bedrock_model_id_cheap`）。
- **测试先行 (TDD)**：每个 Task 先写失败测试 → 跑红 → 最小实现 → 跑绿 → 提交。AWS/LLM 调用一律 mock，测试不触网。
- **提交纪律**：提交信息结尾附 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。Code Defender 拦截时用 `git commit --no-verify`（本仓库既定约定）。**E2E + 主人确认前绝不 push**（Task 7.2 有显式 STOP 门）。
- **语法校验**：Python 改动后跑 `python3 -m py_compile <file>`；前端改动后跑 `cd src/agenticops/web/frontend && npx tsc --noEmit`。
- **python 环境**：`source .venv/bin/activate`。全量 pytest 用 `python -m pytest tests/ --ignore=tests/integration -o addopts=""`（约 103s；跑前 `pkill -9 -f "pytest tests"` 清孤儿进程）。

---

## File Structure

**新建：**
- `src/agenticops/security/advisor.py` — 证据接地建议器（唯一 LLM 环节）：`_parse_recommendations` / `_grounded` / `_build_prompt` / `_critic` / `recommend`。
- `src/agenticops/services/security_service.py` — Security API 查询层（镜像 `services/cost_service.py` 的角色）：summary/trend/findings/recommendations/attack_paths。
- `src/agenticops/web/routers/security.py` — 5 个 GET 端点（镜像 `routers/cost.py`：无 prefix、handler 内 lazy import service、返回 plain dict）。
- `src/agenticops/web/frontend/src/hooks/useSecurity.ts` — 5 个 TanStack Query hooks（镜像 `useGalaxy.ts`）。
- `src/agenticops/web/frontend/src/pages/Security.tsx` — 评分卡 + 趋势线 + 类别雷达 + 发现/建议/暴露链列表。
- `tests/test_security_poll.py`、`tests/test_security_advisor.py`、`tests/test_security_api.py`、`tests/test_security_report.py`。

**修改：**
- `src/agenticops/models.py` — 末尾追加 `SecurityPollCursor`（库中无通用 KV 表，cursor 需持久化）。
- `src/agenticops/security/incremental_poll.py` — stub → 完整实现（cursor helpers + 3 poller + 编排）。
- `src/agenticops/security/posture_snapshot.py` — 加 `_aggregate_topology` / `_build_exposure_paths` / `_emit_posture_signals`；`run_posture_snapshot` 接线 exposure_paths + gate + advisor。
- `src/agenticops/graph/collectors.py` — `collect_vpc_compute` 加 `account: str = ""` 参数（当前裸走默认账户，多账户下违反凭证铁律）。
- `src/agenticops/report/generator.py` — 加 `generate_security_review_report`。
- `src/agenticops/web/app.py` — include security router（~line 311 signals router 后）；report dispatch elif（~line 3221）；pipeline-options 列表加 2 个 security pipeline（line 3554）。
- `src/agenticops/web/schemas.py:172` — report_type 正则加 `security-review`。
- `src/agenticops/tools/report_tools.py:41` — `valid_types` 集合加 `"security-review"`。
- `src/agenticops/agents/reporter_agent.py` — 提示词 bullet（line 38-46 区）+ 工具 docstring（line 114）加 security-review。
- 前端：`api/types.ts`（末尾追加）、`App.tsx`（lazy import + route）、`components/layout/NavItems.tsx`（nav 项 + shield 图标）、`locales/en.json`+`zh.json`（nav.security + dashboard.security*）、`pages/Dashboard.tsx`（高亮卡）、`pages/Reports.tsx:10`（TYPE_COLORS）。

**已核实的既有接口（执行者直接消费，勿重查）：**
- `signal_gate.SignalInput`（`signal_gate.py:113`）：`source, title, description, severity, resource_id="", account_id="", provider="aws", issue_type="other", upstream_key="", kind="alert", metric_data={}, related_changes=[], alarm_name="", raw={}, trace_id=None, im_origin=None, auto_rca=True, detected_by="detect_agent"`。`process_signal(sig) -> GateDecision`（`disposition: promoted|merged|noise`）。`account_id` 字符串只进指纹；HealthIssue.account_id 由 `_promote` 从资源库存/单账户回退解析。
- `ISSUE_TYPES` 已含 `security_exposure`、`security_finding`、`iam_risk`、`config_drift`（`signal_gate.py:45-50`）。
- `signal_gate._call_bedrock(prompt, model_id, max_tokens=500) -> (text, {"input": int, "output": int})`（temp=0）；`_parse_verdict(text) -> Optional[dict]`（剥 code fence 提取 JSON object）。
- `PostureFinding(category, control_id, resource_id, resource_type, raw_check, severity_hint="medium")`；`ScoreResult(overall_score, category_scores, cis_results, metrics)`。
- `reachability.annotate(findings, instances, subnets, security_groups, nacls=None) -> [{finding, reachability, path, port}]`（非网络类 `reachability="n/a"`）。
- `collectors.collect_network_acls(account, region, vpc_id="") -> {subnet_id: {nacl_id, inbound, outbound}}`（vpc_id 空 = 整 region）。
- `collectors._enabled_regions(account) -> list[str]`、`collectors._get_client(service, region, account)`。
- `posture_snapshot._resolve_security_accounts() -> list[str]`（账户 NAME 列表）。
- `analyze_vpc_topology(region, vpc_id, account="") -> str`（JSON：`subnets`（含 `subnet_id/type/default_route_target/map_public_ip_on_launch`）、`security_group_dependency_map`（含 `inbound_rules/outbound_rules`））。
- `graph/collectors.collect_vpc_compute(region, vpc_id) -> {"ec2_instances": [{instance_id, state, subnet_id, public_ip, security_group_ids, ...}], ...}`（Task 4.6 加 account 参数）。
- `ReportGenerator(account: Optional[CloudAccount])`；`self._save_report(report_type, title, content) -> Report`（`generator.py:541`）；方法内 lazy import 是该文件既定模式。
- `SecurityRecommendation` model 已存在（Stage 0）：`snapshot_id(FK,nullable), account_id, category, title, detail, evidence_refs(JSON), severity, critic_verdict, confidence, status`。
- cost router 模式（`routers/cost.py`）：`router = APIRouter()` 无 prefix，完整路径写在装饰器，handler 内 lazy import service，返回 plain dict。include 模式见 `app.py:292-311`。
- API 测试模式（`tests/test_cost_api.py`）：模块级 `client = TestClient(app)` + `patch("agenticops.services.X.fn")`。
- 前端：`apiFetch<T>(path)` 自动加 `/api` 前缀（galaxy hook 传 `"/galaxy/overview"`）；`NAV_ITEMS` 项形如 `{ id, to, icon, labelKey, end }`，`ICON_PATHS` 无 `shield` 需新增；Dashboard kpis 数组在 `Dashboard.tsx:50-55`，高亮块插在 `<InteractionStats />`（line 84）与 `{/* 4. Row 1 */}`（line 86）之间；locales 为**扁平点号 key** JSON；recharts 3 在 package.json，`RadarChart/Radar/PolarGrid` 可用未引入。

---

## Stage 4 — 快频增量采集 + signal_gate 接入 + 慢频接线

> **可验证产出：** `run_incremental_poll()` 按 cursor 增量拉三源 → SignalInput → HealthIssue（issues 列表分钟级可见）；`run_posture_snapshot()` 的 exposure_paths 填充真实可达性结果，可执行发现入 gate。

### Task 4.1: SecurityPollCursor 模型 + cursor 读写 helpers

**Files:**
- Modify: `src/agenticops/models.py`（文件末尾 `SecurityRecommendation` 之后）
- Modify: `src/agenticops/security/incremental_poll.py`
- Test: `tests/test_security_poll.py`

**Interfaces:**
- Produces: `SecurityPollCursor(id, account_id, source, region, cursor, updated_at)`，unique `(account_id, source, region)`；`incremental_poll._get_cursor(session, account, source, region) -> str`（无记录 → 默认 now-24h ISO，首轮 bounded 回填）；`incremental_poll._set_cursor(session, account, source, region, value) -> None`（upsert）。
- Consumes: `models.py` 已 import 的 `Base/Mapped/mapped_column/String/DateTime/UniqueConstraint`（`AgentMemoryFact` 已用 UniqueConstraint，import 已在；实现前 grep 确认，缺则补）。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_security_poll.py
"""Stage 4: cursor-based incremental security polling."""
from contextlib import contextmanager
from datetime import datetime, timedelta, timezone
from unittest.mock import MagicMock, patch

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from agenticops import models


@pytest.fixture
def sess_factory():
    engine = create_engine("sqlite:///:memory:")
    models.Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)

    @contextmanager
    def _sess():
        s = Session()
        try:
            yield s
            s.commit()
        finally:
            s.close()

    return _sess


class TestCursorStore:
    def test_get_cursor_default_is_recent_iso(self, sess_factory):
        from agenticops.security.incremental_poll import _get_cursor
        with sess_factory() as s:
            cur = _get_cursor(s, "acct-a", "guardduty", "us-east-1")
        dt = datetime.fromisoformat(cur)
        age = datetime.now(timezone.utc) - dt
        assert timedelta(hours=23) < age < timedelta(hours=25)  # ~24h backfill

    def test_set_then_get_roundtrip(self, sess_factory):
        from agenticops.security.incremental_poll import _get_cursor, _set_cursor
        with sess_factory() as s:
            _set_cursor(s, "acct-a", "guardduty", "us-east-1", "2026-08-31T01:00:00+00:00")
        with sess_factory() as s:
            assert _get_cursor(s, "acct-a", "guardduty", "us-east-1") == "2026-08-31T01:00:00+00:00"

    def test_set_cursor_upserts_single_row(self, sess_factory):
        from agenticops.security.incremental_poll import _set_cursor
        with sess_factory() as s:
            _set_cursor(s, "acct-a", "securityhub", "us-east-1", "2026-08-31T01:00:00+00:00")
        with sess_factory() as s:
            _set_cursor(s, "acct-a", "securityhub", "us-east-1", "2026-08-31T02:00:00+00:00")
        with sess_factory() as s:
            rows = s.query(models.SecurityPollCursor).all()
            assert len(rows) == 1
            assert rows[0].cursor == "2026-08-31T02:00:00+00:00"
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_poll.py::TestCursorStore -v`
Expected: FAIL — `AttributeError: module 'agenticops.models' has no attribute 'SecurityPollCursor'`

- [ ] **Step 3: 实现**

`models.py` 末尾（`SecurityRecommendation` class 之后）追加：

```python
class SecurityPollCursor(Base):
    """Incremental-poll cursor per (account, source, region) — ISO8601 string."""

    __tablename__ = "security_poll_cursors"
    __table_args__ = (
        UniqueConstraint("account_id", "source", "region", name="uq_security_poll_cursor"),
    )

    id: Mapped[int] = mapped_column(primary_key=True)
    account_id: Mapped[str] = mapped_column(String(64), index=True)
    source: Mapped[str] = mapped_column(String(32))  # guardduty | securityhub | cloudtrail
    region: Mapped[str] = mapped_column(String(32))
    cursor: Mapped[str] = mapped_column(String(64), default="")
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, default=lambda: datetime.now(timezone.utc),
        onupdate=lambda: datetime.now(timezone.utc),
    )
```

`incremental_poll.py` 替换整文件为：

```python
"""Fast-frequency security incremental poll job (SecurityIncrementalPoll).

Cursor-based GuardDuty / SecurityHub / CloudTrail high-risk-event polling.
Every source is fail-soft: on error the source is skipped for this round and
its cursor is NOT advanced (retry next round, no data loss). All AWS calls go
through the provider layer for the target account — never ambient.
"""
from __future__ import annotations

import logging
from datetime import datetime, timedelta, timezone

logger = logging.getLogger(__name__)

_BACKFILL_HOURS = 24  # first run: bounded backfill window


def _get_cursor(session, account: str, source: str, region: str) -> str:
    """Stored cursor, or now-24h ISO on first run (bounded backfill)."""
    from agenticops.models import SecurityPollCursor
    row = (session.query(SecurityPollCursor)
           .filter_by(account_id=account, source=source, region=region).first())
    if row and row.cursor:
        return row.cursor
    return (datetime.now(timezone.utc) - timedelta(hours=_BACKFILL_HOURS)).isoformat()


def _set_cursor(session, account: str, source: str, region: str, value: str) -> None:
    from agenticops.models import SecurityPollCursor
    row = (session.query(SecurityPollCursor)
           .filter_by(account_id=account, source=source, region=region).first())
    if row:
        row.cursor = value
    else:
        session.add(SecurityPollCursor(
            account_id=account, source=source, region=region, cursor=value))


def run_incremental_poll() -> int:
    """Return the number of new findings emitted. Filled in by Task 4.5."""
    return 0
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/models.py src/agenticops/security/incremental_poll.py && python -m pytest tests/test_security_poll.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/models.py src/agenticops/security/incremental_poll.py tests/test_security_poll.py
git commit --no-verify -m "feat(security): SecurityPollCursor model + cursor helpers (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.2: SecurityEvent dataclass + GuardDuty poller

**Files:**
- Modify: `src/agenticops/security/incremental_poll.py`
- Test: `tests/test_security_poll.py`

**Interfaces:**
- Produces:
  - `@dataclass SecurityEvent(source: str, event_id: str, title: str, description: str, severity: str, resource_id: str, resource_type: str, occurred_at: str, raw: dict = {})`
  - `poll_guardduty(account: str, region: str, since_iso: str) -> list[SecurityEvent]`（**API 错误直接 raise**，由编排层决定 cursor 不前移）
  - `_gd_severity(sev: float) -> str`（≥9 critical，≥7 high，≥4 medium，否则 low —— AWS 官方分档）
- Consumes: `agenticops.security.collectors._get_client`。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_security_poll.py`）

```python
def _gd_finding(fid, sev, updated="2026-08-31T02:00:00.000Z", instance="i-1"):
    return {
        "Id": fid, "Severity": sev, "UpdatedAt": updated,
        "Title": "Recon:EC2/PortProbeUnprotectedPort",
        "Description": "EC2 instance has an unprotected port probed.",
        "Resource": {"ResourceType": "Instance",
                     "InstanceDetails": {"InstanceId": instance}},
    }


class TestGuardDutyPoller:
    def _client(self, findings):
        gd = MagicMock()
        gd.list_detectors.return_value = {"DetectorIds": ["det-1"]}
        paginator = MagicMock()
        paginator.paginate.return_value = [{"FindingIds": [f["Id"] for f in findings]}]
        gd.get_paginator.return_value = paginator
        gd.get_findings.return_value = {"Findings": findings}
        return gd

    def test_maps_finding_to_security_event(self):
        from agenticops.security.incremental_poll import poll_guardduty
        gd = self._client([_gd_finding("f-1", 8.0)])
        with patch("agenticops.security.incremental_poll._get_client", return_value=gd):
            out = poll_guardduty("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        assert len(out) == 1
        ev = out[0]
        assert ev.source == "guardduty"
        assert ev.event_id == "f-1"
        assert ev.severity == "high"
        assert ev.resource_id == "i-1"
        assert ev.occurred_at == "2026-08-31T02:00:00.000Z"

    def test_severity_bands(self):
        from agenticops.security.incremental_poll import _gd_severity
        assert _gd_severity(9.1) == "critical"
        assert _gd_severity(7.0) == "high"
        assert _gd_severity(4.5) == "medium"
        assert _gd_severity(2.0) == "low"

    def test_no_detector_returns_empty(self):
        from agenticops.security.incremental_poll import poll_guardduty
        gd = MagicMock()
        gd.list_detectors.return_value = {"DetectorIds": []}
        with patch("agenticops.security.incremental_poll._get_client", return_value=gd):
            assert poll_guardduty("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00") == []

    def test_api_error_propagates(self):
        from agenticops.security.incremental_poll import poll_guardduty
        gd = MagicMock()
        gd.list_detectors.side_effect = RuntimeError("boom")
        with patch("agenticops.security.incremental_poll._get_client", return_value=gd):
            with pytest.raises(RuntimeError):
                poll_guardduty("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_poll.py::TestGuardDutyPoller -v`
Expected: FAIL — `ImportError: cannot import name 'poll_guardduty'`

- [ ] **Step 3: 实现**（`incremental_poll.py`：顶部加 dataclass import + `_get_client`；`run_incremental_poll` 之前加）

```python
from dataclasses import dataclass, field


def _get_client(service: str, region: str, account: str = ""):
    """Provider-layer client for the target account (never ambient)."""
    from agenticops.security.collectors import _get_client as _c
    return _c(service, region, account)


@dataclass
class SecurityEvent:
    """One normalized fast-path security event from an official source."""
    source: str          # guardduty | securityhub | cloudtrail
    event_id: str        # upstream unique id -> SignalInput.upstream_key
    title: str
    description: str
    severity: str        # critical | high | medium | low
    resource_id: str
    resource_type: str
    occurred_at: str     # upstream ISO8601 timestamp
    raw: dict = field(default_factory=dict)


def _gd_severity(sev: float) -> str:
    """AWS GuardDuty numeric severity bands."""
    if sev >= 9.0:
        return "critical"
    if sev >= 7.0:
        return "high"
    if sev >= 4.0:
        return "medium"
    return "low"


def _gd_resource(finding: dict) -> tuple[str, str]:
    res = finding.get("Resource", {}) or {}
    inst = (res.get("InstanceDetails") or {}).get("InstanceId")
    if inst:
        return inst, "Instance"
    key = (res.get("AccessKeyDetails") or {}).get("UserName")
    if key:
        return key, "IAMUser"
    return "unknown", res.get("ResourceType", "unknown")


def poll_guardduty(account: str, region: str, since_iso: str) -> list[SecurityEvent]:
    """GuardDuty findings updated since the cursor. Raises on API error."""
    gd = _get_client("guardduty", region, account)
    out: list[SecurityEvent] = []
    since_ms = int(datetime.fromisoformat(since_iso.replace("Z", "+00:00")).timestamp() * 1000)
    for det in gd.list_detectors().get("DetectorIds", []):
        ids: list[str] = []
        paginator = gd.get_paginator("list_findings")
        for page in paginator.paginate(
            DetectorId=det,
            FindingCriteria={"Criterion": {"updatedAt": {"Gte": since_ms}}},
        ):
            ids.extend(page.get("FindingIds", []))
        for i in range(0, len(ids), 50):
            for f in gd.get_findings(DetectorId=det, FindingIds=ids[i:i + 50]).get("Findings", []):
                rid, rtype = _gd_resource(f)
                out.append(SecurityEvent(
                    source="guardduty", event_id=f.get("Id", ""),
                    title=f.get("Title", "GuardDuty finding"),
                    description=f.get("Description", ""),
                    severity=_gd_severity(float(f.get("Severity", 0))),
                    resource_id=rid, resource_type=rtype,
                    occurred_at=f.get("UpdatedAt", since_iso),
                    raw={"type": f.get("Type", "")},
                ))
    return out
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/incremental_poll.py && python -m pytest tests/test_security_poll.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/incremental_poll.py tests/test_security_poll.py
git commit --no-verify -m "feat(security): SecurityEvent + GuardDuty incremental poller (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.3: SecurityHub poller

**Files:**
- Modify: `src/agenticops/security/incremental_poll.py`
- Test: `tests/test_security_poll.py`

**Interfaces:**
- Produces: `poll_securityhub(account: str, region: str, since_iso: str) -> list[SecurityEvent]`（只拉 `RecordState=ACTIVE` + `WorkflowStatus=NEW`；severity 取 `Severity.Label` 小写，`INFORMATIONAL → low`；API 错误 raise）。

- [ ] **Step 1: 写失败测试**

```python
def _sh_finding(fid, label, updated="2026-08-31T02:00:00Z"):
    return {"Id": fid, "UpdatedAt": updated,
            "Title": "S3 bucket public", "Description": "Bucket allows public read.",
            "Severity": {"Label": label},
            "Resources": [{"Id": "arn:aws:s3:::b1", "Type": "AwsS3Bucket"}]}


class TestSecurityHubPoller:
    def _client(self, findings):
        sh = MagicMock()
        paginator = MagicMock()
        paginator.paginate.return_value = [{"Findings": findings}]
        sh.get_paginator.return_value = paginator
        return sh

    def test_maps_finding(self):
        from agenticops.security.incremental_poll import poll_securityhub
        sh = self._client([_sh_finding("arn:f1", "HIGH")])
        with patch("agenticops.security.incremental_poll._get_client", return_value=sh):
            out = poll_securityhub("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        assert out[0].source == "securityhub"
        assert out[0].severity == "high"
        assert out[0].resource_id == "arn:aws:s3:::b1"

    def test_informational_maps_to_low(self):
        from agenticops.security.incremental_poll import poll_securityhub
        sh = self._client([_sh_finding("arn:f2", "INFORMATIONAL")])
        with patch("agenticops.security.incremental_poll._get_client", return_value=sh):
            out = poll_securityhub("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        assert out[0].severity == "low"

    def test_filters_include_active_new_and_cursor(self):
        from agenticops.security.incremental_poll import poll_securityhub
        sh = self._client([])
        with patch("agenticops.security.incremental_poll._get_client", return_value=sh):
            poll_securityhub("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        filters = sh.get_paginator.return_value.paginate.call_args.kwargs["Filters"]
        assert filters["RecordState"] == [{"Value": "ACTIVE", "Comparison": "EQUALS"}]
        assert filters["WorkflowStatus"] == [{"Value": "NEW", "Comparison": "EQUALS"}]
        assert filters["UpdatedAt"][0]["Start"] == "2026-08-31T00:00:00+00:00"
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_poll.py::TestSecurityHubPoller -v`
Expected: FAIL — `ImportError: cannot import name 'poll_securityhub'`

- [ ] **Step 3: 实现**（`poll_guardduty` 之后）

```python
def poll_securityhub(account: str, region: str, since_iso: str) -> list[SecurityEvent]:
    """SecurityHub ACTIVE+NEW findings updated since the cursor. Raises on API error."""
    sh = _get_client("securityhub", region, account)
    now_iso = datetime.now(timezone.utc).isoformat()
    out: list[SecurityEvent] = []
    paginator = sh.get_paginator("get_findings")
    for page in paginator.paginate(Filters={
        "UpdatedAt": [{"Start": since_iso, "End": now_iso}],
        "RecordState": [{"Value": "ACTIVE", "Comparison": "EQUALS"}],
        "WorkflowStatus": [{"Value": "NEW", "Comparison": "EQUALS"}],
    }):
        for f in page.get("Findings", []):
            res = (f.get("Resources") or [{}])[0]
            label = str((f.get("Severity") or {}).get("Label", "MEDIUM")).lower()
            out.append(SecurityEvent(
                source="securityhub", event_id=f.get("Id", ""),
                title=f.get("Title", "Security Hub finding"),
                description=f.get("Description", ""),
                severity="low" if label == "informational" else label,
                resource_id=res.get("Id", "unknown"),
                resource_type=res.get("Type", "unknown"),
                occurred_at=f.get("UpdatedAt", since_iso),
            ))
    return out
```

- [ ] **Step 4: 跑绿**

Run: `python -m pytest tests/test_security_poll.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/incremental_poll.py tests/test_security_poll.py
git commit --no-verify -m "feat(security): SecurityHub incremental poller (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.4: CloudTrail 高危事件 poller

**Files:**
- Modify: `src/agenticops/security/incremental_poll.py`
- Test: `tests/test_security_poll.py`

**Interfaces:**
- Produces: `HIGH_RISK_EVENTS: frozenset[str]`；`poll_cloudtrail(account: str, region: str, since_iso: str) -> list[SecurityEvent]`（`lookup_events` 拉全量再客户端过滤事件名——LookupAttributes 一次只支持一个，无法服务端多事件名过滤；10 分钟窗口量级可控；severity 恒 `high`）。

- [ ] **Step 1: 写失败测试**

```python
def _ct_event(name, eid="e-1", when=None, username="alice"):
    return {"EventId": eid, "EventName": name, "Username": username,
            "EventTime": when or datetime(2026, 8, 31, 2, 0, tzinfo=timezone.utc),
            "Resources": [{"ResourceName": "trail-1", "ResourceType": "AWS::CloudTrail::Trail"}]}


class TestCloudTrailPoller:
    def _client(self, events):
        ct = MagicMock()
        paginator = MagicMock()
        paginator.paginate.return_value = [{"Events": events}]
        ct.get_paginator.return_value = paginator
        return ct

    def test_high_risk_event_captured(self):
        from agenticops.security.incremental_poll import poll_cloudtrail
        ct = self._client([_ct_event("StopLogging")])
        with patch("agenticops.security.incremental_poll._get_client", return_value=ct):
            out = poll_cloudtrail("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        assert len(out) == 1
        assert out[0].source == "cloudtrail"
        assert out[0].severity == "high"
        assert "StopLogging" in out[0].title
        assert out[0].resource_id == "trail-1"

    def test_benign_event_filtered_out(self):
        from agenticops.security.incremental_poll import poll_cloudtrail
        ct = self._client([_ct_event("DescribeInstances")])
        with patch("agenticops.security.incremental_poll._get_client", return_value=ct):
            out = poll_cloudtrail("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        assert out == []

    def test_username_fallback_resource(self):
        from agenticops.security.incremental_poll import poll_cloudtrail
        ev = _ct_event("CreateAccessKey")
        ev["Resources"] = []
        ct = self._client([ev])
        with patch("agenticops.security.incremental_poll._get_client", return_value=ct):
            out = poll_cloudtrail("acct-a", "us-east-1", "2026-08-31T00:00:00+00:00")
        assert out[0].resource_id == "alice"
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_poll.py::TestCloudTrailPoller -v`
Expected: FAIL — `ImportError: cannot import name 'poll_cloudtrail'`

- [ ] **Step 3: 实现**（`poll_securityhub` 之后）

```python
HIGH_RISK_EVENTS = frozenset({
    # logging tampering
    "DeleteTrail", "StopLogging", "UpdateTrail", "PutEventSelectors", "DeleteFlowLogs",
    # network exposure
    "AuthorizeSecurityGroupIngress",
    # identity escalation
    "CreateUser", "CreateAccessKey", "CreateLoginProfile", "AttachUserPolicy",
    "PutUserPolicy", "DeactivateMFADevice",
    # data exposure
    "PutBucketAcl", "PutBucketPolicy", "DeleteBucketPolicy",
    # encryption tampering
    "DisableKey", "ScheduleKeyDeletion",
})


def poll_cloudtrail(account: str, region: str, since_iso: str) -> list[SecurityEvent]:
    """High-risk CloudTrail management events since the cursor. Raises on API error.

    lookup_events supports only one LookupAttribute per call, so we pull the
    window and filter client-side against HIGH_RISK_EVENTS."""
    ct = _get_client("cloudtrail", region, account)
    start = datetime.fromisoformat(since_iso.replace("Z", "+00:00"))
    out: list[SecurityEvent] = []
    paginator = ct.get_paginator("lookup_events")
    for page in paginator.paginate(StartTime=start):
        for e in page.get("Events", []):
            name = e.get("EventName", "")
            if name not in HIGH_RISK_EVENTS:
                continue
            res = (e.get("Resources") or [{}])[0]
            user = e.get("Username", "unknown")
            when = e.get("EventTime")
            out.append(SecurityEvent(
                source="cloudtrail", event_id=e.get("EventId", ""),
                title=f"High-risk API call: {name}",
                description=f"{name} invoked by {user}",
                severity="high",
                resource_id=res.get("ResourceName") or user,
                resource_type=res.get("ResourceType", "unknown"),
                occurred_at=when.isoformat() if hasattr(when, "isoformat") else str(when or since_iso),
            ))
    return out
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/incremental_poll.py && python -m pytest tests/test_security_poll.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/incremental_poll.py tests/test_security_poll.py
git commit --no-verify -m "feat(security): CloudTrail high-risk event poller (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.5: run_incremental_poll 编排（cursor 语义 + SignalInput + 快照查表富化）★

**Files:**
- Modify: `src/agenticops/security/incremental_poll.py`（替换 `run_incremental_poll` stub 体）
- Test: `tests/test_security_poll.py`

**Interfaces:**
- Produces: `run_incremental_poll() -> int`（本轮 emit 的 signal 数）；`_reachability_lookup(account) -> dict[str, dict]`（读最新 SecuritySnapshot.exposure_paths，key 同时含 `resource_id` 与暴露链末元素的 instance-id）；`_emit_signal(event, account, reach_map) -> bool`。
- Consumes: `posture_snapshot._resolve_security_accounts`、`collectors._enabled_regions`、`signal_gate.process_signal`/`SignalInput`、Task 4.1-4.4 全部。
- **Cursor 语义**：poll 前记 `poll_start = now ISO`；源成功（无论有无事件）→ cursor 前移到 `poll_start`；源抛异常 → WARNING + cursor 不动（下轮重拉，指纹去重吸收重叠）。
- **确定性映射**：`_AUTO_RCA = {"guardduty": True, "cloudtrail": True, "securityhub": False}`（事件类 True / 结构类 False，spec §3.6）；`_ISSUE_TYPE = {"guardduty": "security_finding", "securityhub": "security_finding", "cloudtrail": "config_drift"}`。

- [ ] **Step 1: 写失败测试**

```python
class TestRunIncrementalPoll:
    def _setup(self, monkeypatch, sess_factory, pollers):
        import agenticops.security.incremental_poll as ip
        monkeypatch.setattr(ip, "get_db_session", sess_factory, raising=False)
        monkeypatch.setattr(ip, "_resolve_security_accounts", lambda: ["acct-a"])
        monkeypatch.setattr(ip, "_enabled_regions", lambda a: ["us-east-1"])
        monkeypatch.setattr(ip, "_reachability_lookup", lambda a: {})
        monkeypatch.setattr(ip, "_SOURCE_POLLERS", pollers)
        return ip

    def test_signals_emitted_and_cursor_advanced(self, monkeypatch, sess_factory):
        from agenticops.security.incremental_poll import SecurityEvent
        ev = SecurityEvent("guardduty", "f-1", "t", "d", "high", "i-1", "Instance",
                           "2026-08-31T02:00:00+00:00")
        ip = self._setup(monkeypatch, sess_factory, {"guardduty": lambda a, r, s: [ev]})
        calls = []
        with patch("agenticops.services.signal_gate.process_signal",
                   side_effect=lambda sig: calls.append(sig) or MagicMock(disposition="promoted")):
            n = ip.run_incremental_poll()
        assert n == 1
        sig = calls[0]
        assert sig.source == "security_poll"
        assert sig.upstream_key == "f-1"
        assert sig.issue_type == "security_finding"
        assert sig.auto_rca is True
        assert sig.detected_by == "security_poll"
        assert sig.kind == "detection"
        assert sig.account_id == "acct-a"
        from agenticops import models
        with sess_factory() as s:
            row = s.query(models.SecurityPollCursor).filter_by(source="guardduty").one()
            assert row.cursor > "2026-08-31"  # advanced to poll_start (now)

    def test_source_failure_does_not_advance_cursor(self, monkeypatch, sess_factory):
        def _boom(a, r, s):
            raise RuntimeError("api down")
        ip = self._setup(monkeypatch, sess_factory, {"guardduty": _boom})
        with patch("agenticops.services.signal_gate.process_signal") as ps:
            n = ip.run_incremental_poll()
        assert n == 0
        ps.assert_not_called()
        from agenticops import models
        with sess_factory() as s:
            assert s.query(models.SecurityPollCursor).count() == 0

    def test_reachability_from_snapshot_attached(self, monkeypatch, sess_factory):
        from agenticops.security.incremental_poll import SecurityEvent
        ev = SecurityEvent("guardduty", "f-2", "t", "d", "high", "i-9", "Instance",
                           "2026-08-31T02:00:00+00:00")
        ip = self._setup(monkeypatch, sess_factory, {"guardduty": lambda a, r, s: [ev]})
        monkeypatch.setattr(ip, "_reachability_lookup", lambda a: {
            "i-9": {"reachability": "reachable", "path": ["internet", "sn-1", "i-9:22"], "port": 22}})
        calls = []
        with patch("agenticops.services.signal_gate.process_signal",
                   side_effect=lambda sig: calls.append(sig) or MagicMock(disposition="promoted")):
            ip.run_incremental_poll()
        assert calls[0].metric_data["reachability"] == "reachable"

    def test_reachability_lookup_reads_latest_snapshot(self, sess_factory, monkeypatch):
        import agenticops.security.incremental_poll as ip
        from agenticops import models
        monkeypatch.setattr(ip, "get_db_session", sess_factory, raising=False)
        with sess_factory() as s:
            s.add(models.SecuritySnapshot(
                account_id="acct-a", provider="aws", overall_score=50.0,
                exposure_paths=[{"resource_id": "sg-1", "port": 22,
                                 "path": ["internet", "sn-1", "i-7:22"],
                                 "reachability": "reachable"}]))
        m = ip._reachability_lookup("acct-a")
        assert m["sg-1"]["reachability"] == "reachable"
        assert m["i-7"]["reachability"] == "reachable"  # instance id from path tail
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_poll.py::TestRunIncrementalPoll -v`
Expected: FAIL — `AttributeError: ... has no attribute '_SOURCE_POLLERS'`

- [ ] **Step 3: 实现**（`incremental_poll.py`：顶部补 import，替换 `run_incremental_poll`）

顶部 import 区补：

```python
from agenticops.models import get_db_session
from agenticops.security.collectors import _enabled_regions
from agenticops.security.posture_snapshot import _resolve_security_accounts
```

`poll_cloudtrail` 之后：

```python
_SOURCE_POLLERS = {
    "guardduty": poll_guardduty,
    "securityhub": poll_securityhub,
    "cloudtrail": poll_cloudtrail,
}
# Event-like sources warrant auto-RCA; posture/config aggregates do not (spec 3.6).
_AUTO_RCA = {"guardduty": True, "cloudtrail": True, "securityhub": False}
_ISSUE_TYPE = {"guardduty": "security_finding", "securityhub": "security_finding",
               "cloudtrail": "config_drift"}


def _reachability_lookup(account: str) -> dict[str, dict]:
    """{resource_id | instance_id: exposure row} from the account's latest
    snapshot — deterministic enrichment lookup for the fast path. Fail-soft {}."""
    try:
        from agenticops.models import SecuritySnapshot
        with get_db_session() as session:
            snap = (session.query(SecuritySnapshot)
                    .filter_by(account_id=account)
                    .order_by(SecuritySnapshot.created_at.desc()).first())
            if not snap:
                return {}
            out: dict[str, dict] = {}
            for p in snap.exposure_paths or []:
                row = dict(p)
                out[str(p.get("resource_id", ""))] = row
                path = p.get("path") or []
                if path:
                    out[str(path[-1]).split(":", 1)[0]] = row
            out.pop("", None)
            return out
    except Exception as e:
        logger.warning("reachability lookup failed for %s: %s", account, e)
        return {}


def _emit_signal(event: SecurityEvent, account: str, reach_map: dict) -> bool:
    """One SecurityEvent -> signal gate. Fail-soft per event."""
    try:
        from agenticops.services.signal_gate import SignalInput, process_signal
        metric_data = {"security_source": event.source, "occurred_at": event.occurred_at}
        reach = reach_map.get(event.resource_id)
        if reach:
            metric_data.update({"reachability": reach.get("reachability"),
                                "path": reach.get("path"), "port": reach.get("port")})
        process_signal(SignalInput(
            source="security_poll",
            title=event.title[:300],
            description=event.description,
            severity=event.severity,
            resource_id=event.resource_id,
            account_id=account,
            issue_type=_ISSUE_TYPE[event.source],
            upstream_key=event.event_id,
            kind="detection",
            metric_data=metric_data,
            raw=event.raw,
            auto_rca=_AUTO_RCA[event.source],
            detected_by="security_poll",
        ))
        return True
    except Exception as e:
        logger.warning("emit signal failed (%s %s): %s", event.source, event.event_id, e)
        return False


def run_incremental_poll() -> int:
    """Poll every (account x region x source) since its cursor, emit signals.
    A failed source keeps its cursor (retry next round); a successful source
    advances to poll_start even with zero events."""
    emitted = 0
    for account in _resolve_security_accounts():
        reach_map = _reachability_lookup(account)
        for region in _enabled_regions(account):
            for source, poller in _SOURCE_POLLERS.items():
                poll_start = datetime.now(timezone.utc).isoformat()
                with get_db_session() as session:
                    since = _get_cursor(session, account, source, region)
                try:
                    events = poller(account, region, since)
                except Exception as e:
                    logger.warning("%s poll failed for %s/%s: %s (cursor kept)",
                                   source, account, region, e)
                    continue
                for ev in sorted(events, key=lambda e: e.occurred_at):
                    if _emit_signal(ev, account, reach_map):
                        emitted += 1
                with get_db_session() as session:
                    _set_cursor(session, account, source, region, poll_start)
    return emitted
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/incremental_poll.py && python -m pytest tests/test_security_poll.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/incremental_poll.py tests/test_security_poll.py
git commit --no-verify -m "feat(security): run_incremental_poll orchestration -> signal gate (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.6: collect_vpc_compute 账户寻址（凭证铁律补齐）

**Files:**
- Modify: `src/agenticops/graph/collectors.py`（`collect_vpc_compute`，line 21）
- Test: `tests/test_graph_collectors.py`（追加）

**Interfaces:**
- Produces: `collect_vpc_compute(region: str, vpc_id: str, account: str = "") -> dict`——签名加第三个可选参数，函数体内**每一处** `_get_client(service, region)` 调用改为 `_get_client(service, region, account)`（EC2/RDS/Lambda/ELBv2/ElastiCache 全部）。默认 `""` 保持既有调用方（`graph/tools.py:45`、`graph/api.py:77`、全部既有测试）行为不变。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_graph_collectors.py`）

```python
class TestAccountAddressing:
    def test_collect_vpc_compute_threads_account_to_all_clients(self):
        from unittest.mock import MagicMock, patch
        from agenticops.graph.collectors import collect_vpc_compute
        client = MagicMock()
        paginator = MagicMock()
        paginator.paginate.return_value = []
        client.get_paginator.return_value = paginator
        client.list_functions.return_value = {"Functions": []}
        client.describe_target_groups.return_value = {"TargetGroups": []}
        client.describe_cache_clusters.return_value = {"CacheClusters": []}
        with patch("agenticops.graph.collectors._get_client", return_value=client) as gc:
            collect_vpc_compute("us-east-1", "vpc-1", account="acct-b")
        assert gc.call_count >= 2
        for call in gc.call_args_list:
            assert call.args[2] == "acct-b" or call.kwargs.get("account") == "acct-b"
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_graph_collectors.py::TestAccountAddressing -v`
Expected: FAIL — `TypeError: collect_vpc_compute() got an unexpected keyword argument 'account'`

- [ ] **Step 3: 实现**

签名改为 `def collect_vpc_compute(region: str, vpc_id: str, account: str = "") -> dict[str, Any]:`，函数体内逐个把 `_get_client("ec2", region)`、`_get_client("rds", region)`、`_get_client("lambda", region)`、`_get_client("elbv2", region)`、`_get_client("elasticache", region)` 改为带 `, account`（用 grep 确认函数体内没有漏改：`sed -n '21,193p' src/agenticops/graph/collectors.py | grep _get_client` 每行都带 account）。

- [ ] **Step 4: 跑绿（含既有回归）**

Run: `python3 -m py_compile src/agenticops/graph/collectors.py && python -m pytest tests/test_graph_collectors.py -v`
Expected: PASS（新增 1 + 既有全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/graph/collectors.py tests/test_graph_collectors.py
git commit --no-verify -m "feat(security): account-addressed collect_vpc_compute (credential rule) (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.7: _aggregate_topology 聚合（instances/subnets/SGs/NACLs）

**Files:**
- Modify: `src/agenticops/security/posture_snapshot.py`
- Test: `tests/test_security_snapshot.py`（追加）

**Interfaces:**
- Produces: `_aggregate_topology(account: str) -> tuple[dict, dict, dict, dict]` = `(instances, subnets, security_groups, nacls)`：
  - `instances`: `{instance_id: instance_dict}`（`collect_vpc_compute` 的 `ec2_instances` 元素）
  - `subnets`: `{subnet_id: subnet_dict}`（`analyze_vpc_topology` JSON 的 `subnets` 元素）
  - `security_groups`: `{sg_id: dep_map_entry}`（JSON 的 `security_group_dependency_map`，含 `inbound_rules`）
  - `nacls`: `{subnet_id: {nacl_id, inbound, outbound}}`（`collect_network_acls(account, region)` 整 region 合并）
  - 每个 region/vpc fail-soft：异常 WARNING + continue，返回已聚合部分。
- Consumes: `collectors._enabled_regions`/`_get_client`/`collect_network_acls`、`network_tools.analyze_vpc_topology(region, vpc_id, account)`（JSON 字符串）、`graph.collectors.collect_vpc_compute(region, vpc_id, account)`（Task 4.6）。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_security_snapshot.py`）

```python
import json
from unittest.mock import MagicMock, patch


class TestAggregateTopology:
    def test_aggregates_across_vpcs(self, monkeypatch):
        import agenticops.security.posture_snapshot as ps
        topo = {"subnets": [{"subnet_id": "sn-1", "type": "public",
                             "default_route_target": "igw-1", "map_public_ip_on_launch": True}],
                "security_group_dependency_map": {
                    "sg-1": {"name": "web", "inbound_rules": [
                        {"protocol": "tcp", "ports": "22", "sources": ["0.0.0.0/0"]}]}}}
        compute = {"ec2_instances": [{"instance_id": "i-1", "state": "running",
                                      "public_ip": "1.2.3.4", "subnet_id": "sn-1",
                                      "security_group_ids": ["sg-1"]}]}
        ec2 = MagicMock()
        ec2.describe_vpcs.return_value = {"Vpcs": [{"VpcId": "vpc-1"}]}
        monkeypatch.setattr(ps, "_enabled_regions", lambda a: ["us-east-1"], raising=False)
        with patch("agenticops.security.posture_snapshot._agg_get_client", return_value=ec2), \
             patch("agenticops.tools.network_tools.analyze_vpc_topology",
                   return_value=json.dumps(topo)), \
             patch("agenticops.graph.collectors.collect_vpc_compute", return_value=compute), \
             patch("agenticops.security.collectors.collect_network_acls",
                   return_value={"sn-1": {"nacl_id": "acl-1", "inbound": [], "outbound": []}}):
            instances, subnets, sgs, nacls = ps._aggregate_topology("acct-a")
        assert instances["i-1"]["subnet_id"] == "sn-1"
        assert subnets["sn-1"]["type"] == "public"
        assert sgs["sg-1"]["inbound_rules"][0]["ports"] == "22"
        assert nacls["sn-1"]["nacl_id"] == "acl-1"

    def test_region_failure_is_isolated(self, monkeypatch):
        import agenticops.security.posture_snapshot as ps
        monkeypatch.setattr(ps, "_enabled_regions", lambda a: ["bad-1", "us-east-1"], raising=False)
        good_ec2 = MagicMock()
        good_ec2.describe_vpcs.return_value = {"Vpcs": []}

        def _client(service, region, account):
            if region == "bad-1":
                raise RuntimeError("region down")
            return good_ec2
        with patch("agenticops.security.posture_snapshot._agg_get_client", side_effect=_client), \
             patch("agenticops.security.collectors.collect_network_acls", return_value={}):
            instances, subnets, sgs, nacls = ps._aggregate_topology("acct-a")
        assert (instances, subnets, sgs, nacls) == ({}, {}, {}, {})
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_snapshot.py::TestAggregateTopology -v`
Expected: FAIL — `AttributeError: ... has no attribute '_aggregate_topology'`

- [ ] **Step 3: 实现**（`posture_snapshot.py`：`_prune_old_snapshots` 之后）

```python
def _agg_get_client(service: str, region: str, account: str):
    """Indirection point so tests can patch the provider-layer client."""
    from agenticops.security.collectors import _get_client
    return _get_client(service, region, account)


def _aggregate_topology(account: str) -> tuple[dict, dict, dict, dict]:
    """(instances, subnets, security_groups, nacls) across enabled regions.
    Fail-soft per region and per VPC — partial data is fine (reachability marks
    anything it cannot resolve as 'undetermined', never 'not_reachable')."""
    import json as _json

    from agenticops.security.collectors import _enabled_regions, collect_network_acls

    instances: dict = {}
    subnets: dict = {}
    sgs: dict = {}
    nacls: dict = {}
    for region in _enabled_regions(account):
        try:
            ec2 = _agg_get_client("ec2", region, account)
            vpc_ids = [v["VpcId"] for v in ec2.describe_vpcs().get("Vpcs", [])]
            nacls.update(collect_network_acls(account, region))
        except Exception as e:
            logger.warning("topology region %s failed for %s: %s", region, account, e)
            continue
        for vpc_id in vpc_ids:
            try:
                from agenticops.graph.collectors import collect_vpc_compute
                from agenticops.tools.network_tools import analyze_vpc_topology
                topo = _json.loads(analyze_vpc_topology(region, vpc_id, account))
                for sn in topo.get("subnets", []) or []:
                    if sn.get("subnet_id"):
                        subnets[sn["subnet_id"]] = sn
                sgs.update(topo.get("security_group_dependency_map", {}) or {})
                comp = collect_vpc_compute(region, vpc_id, account)
                for inst in comp.get("ec2_instances", []) or []:
                    if inst.get("instance_id"):
                        instances[inst["instance_id"]] = inst
            except Exception as e:
                logger.warning("topology vpc %s/%s failed for %s: %s",
                               region, vpc_id, account, e)
    return instances, subnets, sgs, nacls
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/posture_snapshot.py && python -m pytest tests/test_security_snapshot.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/posture_snapshot.py tests/test_security_snapshot.py
git commit --no-verify -m "feat(security): _aggregate_topology instances/subnets/SGs/NACLs (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4.8: 慢频接线——exposure_paths 落库 + 可执行发现入 gate ★

**Files:**
- Modify: `src/agenticops/security/posture_snapshot.py`（`run_posture_snapshot` 体）
- Test: `tests/test_security_snapshot.py`（追加）

**Interfaces:**
- Produces:
  - `_build_exposure_paths(annotated: list[dict], cap: int = 50) -> list[dict]`——`[{resource_id, port, path, reachability}]`，只留 `reachability != "n/a"`，排序 reachable → undetermined → not_reachable（同态按 resource_id 字典序，保证可复现），截断 cap。
  - `_emit_posture_signals(account: str, annotated: list[dict]) -> int`——只对**可执行发现**（`finding.severity_hint == "high"` 或 `reachability == "reachable"`）产 Signal：`source="security_posture"`, `issue_type` 按 `_CATEGORY_ISSUE_TYPE = {"iam": "iam_risk", "network": "security_exposure", "data": "security_exposure", "logging": "security_finding"}`，`upstream_key=f"{control_id}|{resource_id}"`，`auto_rca=False`，`detected_by="security_posture"`，`metric_data={"control_id", "reachability", "path", "port"}`。逐条 fail-soft。
  - `run_posture_snapshot` 行为增量：聚合拓扑 → `annotate(findings, ...)`（聚合/标注失败 → `annotated=[]`，快照照常落库）→ `exposure_paths=_build_exposure_paths(annotated)` 落库 → `_emit_posture_signals`。**评分入参不变**（可达性不进评分，原则 4）。
- Consumes: Task 4.7 `_aggregate_topology`、`reachability.annotate`、`signal_gate.SignalInput/process_signal`。

- [ ] **Step 1: 写失败测试**（追加；沿用本文件既有 `TestRunPostureSnapshot` 的 in-memory DB 模式）

```python
from agenticops.security.collectors import PostureFinding


def _annotated(reach="reachable"):
    f = PostureFinding("network", "cis-4.1", "sg-1", "SecurityGroup", "0.0.0.0/0:22", "high")
    return [{"finding": f, "reachability": reach,
             "path": ["internet", "sn-1", "i-1:22"], "port": 22}]


class TestExposureWiring:
    def test_build_exposure_paths_sorted_and_filtered(self):
        import agenticops.security.posture_snapshot as ps
        f_net = PostureFinding("network", "cis-4.1", "sg-b", "SG", "open", "high")
        f_net2 = PostureFinding("network", "cis-4.2", "sg-a", "SG", "open", "high")
        f_iam = PostureFinding("iam", "cis-1.3", "alice", "IAMUser", "stale")
        rows = ps._build_exposure_paths([
            {"finding": f_net, "reachability": "not_reachable", "path": [], "port": 22},
            {"finding": f_net2, "reachability": "reachable",
             "path": ["internet", "sn-1", "i-1:3389"], "port": 3389},
            {"finding": f_iam, "reachability": "n/a", "path": [], "port": None},
        ])
        assert [r["reachability"] for r in rows] == ["reachable", "not_reachable"]
        assert rows[0]["resource_id"] == "sg-a"

    def test_emit_only_actionable(self):
        import agenticops.security.posture_snapshot as ps
        low = PostureFinding("data", "cis-enc", "vol-1", "EBSVolume", "unencrypted")  # medium
        high = PostureFinding("iam", "cis-1.4", "<root_account>", "IAMRoot", "root key", "high")
        annotated = [
            {"finding": low, "reachability": "n/a", "path": [], "port": None},
            {"finding": high, "reachability": "n/a", "path": [], "port": None},
        ]
        calls = []
        with patch("agenticops.services.signal_gate.process_signal",
                   side_effect=lambda sig: calls.append(sig) or MagicMock()):
            n = ps._emit_posture_signals("acct-a", annotated)
        assert n == 1
        assert calls[0].issue_type == "iam_risk"
        assert calls[0].auto_rca is False
        assert calls[0].detected_by == "security_posture"
        assert calls[0].upstream_key == "cis-1.4|<root_account>"

    def test_snapshot_gets_exposure_paths_and_survives_topology_failure(self, monkeypatch):
        import agenticops.security.posture_snapshot as ps
        from agenticops import models
        from sqlalchemy import create_engine
        from sqlalchemy.orm import sessionmaker
        from contextlib import contextmanager
        engine = create_engine("sqlite:///:memory:")
        models.Base.metadata.create_all(engine)
        Session = sessionmaker(bind=engine)

        @contextmanager
        def _sess():
            s = Session()
            try:
                yield s
                s.commit()
            finally:
                s.close()

        monkeypatch.setattr(ps, "get_db_session", _sess)
        monkeypatch.setattr(ps, "_resolve_security_accounts", lambda: ["acct-a"])
        f = PostureFinding("network", "cis-4.1", "sg-1", "SecurityGroup", "open", "high")
        monkeypatch.setattr(ps, "collect_posture", lambda a: [f])
        monkeypatch.setattr(ps, "_aggregate_topology", lambda a: ({}, {}, {}, {}))
        monkeypatch.setattr(ps, "_emit_posture_signals", lambda a, ann: 0)
        with patch("agenticops.security.posture_snapshot.annotate",
                   return_value=_annotated("reachable")):
            n = ps.run_posture_snapshot()
        assert n == 1
        with _sess() as s:
            snap = s.query(models.SecuritySnapshot).one()
            assert snap.exposure_paths[0]["reachability"] == "reachable"

        # topology aggregation blowing up must not kill the snapshot
        def _boom(a):
            raise RuntimeError("topology down")
        monkeypatch.setattr(ps, "_aggregate_topology", _boom)
        n = ps.run_posture_snapshot()
        assert n == 1  # snapshot still written, exposure_paths []
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_snapshot.py::TestExposureWiring -v`
Expected: FAIL — `AttributeError: ... has no attribute '_build_exposure_paths'`

- [ ] **Step 3: 实现**（`posture_snapshot.py`）

顶部 import 区补：

```python
from agenticops.security.reachability import annotate
```

`_aggregate_topology` 之后加：

```python
_REACH_ORDER = {"reachable": 0, "undetermined": 1, "not_reachable": 2}
_CATEGORY_ISSUE_TYPE = {"iam": "iam_risk", "network": "security_exposure",
                        "data": "security_exposure", "logging": "security_finding"}


def _build_exposure_paths(annotated: list, cap: int = 50) -> list[dict]:
    """Reproducible exposure rows for the snapshot: reachable first, then
    undetermined, then not_reachable; ties by resource_id. n/a rows dropped."""
    rows = [{"resource_id": a["finding"].resource_id, "port": a["port"],
             "path": a["path"], "reachability": a["reachability"]}
            for a in annotated if a["reachability"] != "n/a"]
    rows.sort(key=lambda r: (_REACH_ORDER.get(r["reachability"], 3), str(r["resource_id"])))
    return rows[:cap]


def _is_actionable(entry: dict) -> bool:
    """Actionable finding (spec 'executable'): high severity, or proven-open path."""
    return entry["finding"].severity_hint == "high" or entry["reachability"] == "reachable"


def _emit_posture_signals(account: str, annotated: list) -> int:
    """Actionable posture findings -> signal gate (auto_rca off). Fail-soft per finding."""
    emitted = 0
    for entry in annotated:
        if not _is_actionable(entry):
            continue
        f = entry["finding"]
        try:
            from agenticops.services.signal_gate import SignalInput, process_signal
            process_signal(SignalInput(
                source="security_posture",
                title=f"[{f.control_id}] {f.resource_type} {f.resource_id}: {f.raw_check}"[:300],
                description=f.raw_check,
                severity=f.severity_hint,
                resource_id=f.resource_id,
                account_id=account,
                issue_type=_CATEGORY_ISSUE_TYPE.get(f.category, "security_finding"),
                upstream_key=f"{f.control_id}|{f.resource_id}",
                kind="detection",
                metric_data={"control_id": f.control_id,
                             "reachability": entry["reachability"],
                             "path": entry["path"], "port": entry["port"]},
                auto_rca=False,
                detected_by="security_posture",
            ))
            emitted += 1
        except Exception as e:
            logger.warning("posture signal failed (%s %s): %s", f.control_id, f.resource_id, e)
    return emitted
```

`run_posture_snapshot` 的 per-account try 块改为（评分入参不变）：

```python
        try:
            findings = collect_posture(account)
            result = score(findings)
            annotated: list = []
            try:
                instances, subnets, sgs, nacls = _aggregate_topology(account)
                annotated = annotate(findings, instances, subnets, sgs, nacls=nacls)
            except Exception as e:
                logger.warning("reachability annotation failed for %s: %s "
                               "(snapshot written without exposure paths)", account, e)
            with get_db_session() as session:
                session.add(SecuritySnapshot(
                    account_id=account, provider="aws",
                    overall_score=result.overall_score,
                    category_scores=result.category_scores,
                    metrics=result.metrics,
                    exposure_paths=_build_exposure_paths(annotated),
                    cis_results=result.cis_results,
                ))
                _prune_old_snapshots(session)
            _emit_posture_signals(account, annotated)
            written += 1
        except Exception as e:
            logger.warning("posture snapshot failed for account %s: %s", account, e)
```

- [ ] **Step 4: 跑绿（含 Stage 0-3 全量安全回归）**

Run: `python3 -m py_compile src/agenticops/security/posture_snapshot.py && python -m pytest tests/test_security_snapshot.py tests/test_security_poll.py tests/test_security_reachability.py tests/test_security_scoring.py tests/test_security_collectors.py tests/test_security_scheduler.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/posture_snapshot.py tests/test_security_snapshot.py
git commit --no-verify -m "feat(security): exposure_paths wiring + actionable posture findings -> gate (Stage 4)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

> **阶段 4 验证锚点：** 快照带真实 exposure_paths；快频/慢频发现统一经 signal_gate 折叠为 HealthIssue（issues 列表可见）；cursor 失败不前移。

---

## Stage 5 — 证据接地建议器 + critic

> **可验证产出：** 低分类别产出 grounded 建议入 `SecurityRecommendation`；不接地/refuted/异常 → 0 行（fail-closed）。

### Task 5.1: advisor 纯函数（解析 / 接地 / 提示词）

**Files:**
- Create: `src/agenticops/security/advisor.py`
- Test: `tests/test_security_advisor.py`

**Interfaces:**
- Produces:
  - `_parse_recommendations(text: str) -> list[dict]`——剥 code fence 提取首个 JSON **数组**；非法/非数组 → `[]`。
  - `_grounded(recs: list[dict], resource_ids: set[str]) -> list[dict]`——保留"`evidence_refs` 非空 **且** 每个 ref 都在本轮采集的 resource_ids 集合内"的建议（精确成员校验，比 `_ref_in_trace` 的子串校验更严 —— 建议的证据必须逐字对应采集到的资源）。
  - `_build_prompt(account: str, category_scores: dict, findings: list) -> str`——低分类别 + 每条 finding 一行证据 `resource_id — raw_check (control_id)`；要求输出 JSON 数组 `[{category, title, detail, evidence_refs, severity, confidence}]`。
- Consumes: `PostureFinding`。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_security_advisor.py
"""Stage 5: evidence-grounded LLM advisor (fail-closed)."""
from unittest.mock import MagicMock, patch

from agenticops.security.advisor import _build_prompt, _grounded, _parse_recommendations
from agenticops.security.collectors import PostureFinding


class TestParseRecommendations:
    def test_parses_fenced_json_array(self):
        text = "```json\n[{\"category\": \"iam\", \"title\": \"t\"}]\n```"
        assert _parse_recommendations(text) == [{"category": "iam", "title": "t"}]

    def test_garbage_returns_empty(self):
        assert _parse_recommendations("no json here") == []

    def test_json_object_not_array_returns_empty(self):
        assert _parse_recommendations('{"category": "iam"}') == []


class TestGrounding:
    def test_unknown_ref_dropped(self):
        recs = [{"title": "t", "evidence_refs": ["sg-1", "sg-GHOST"]}]
        assert _grounded(recs, {"sg-1"}) == []

    def test_empty_refs_dropped(self):
        assert _grounded([{"title": "t", "evidence_refs": []}], {"sg-1"}) == []

    def test_fully_grounded_kept(self):
        recs = [{"title": "t", "evidence_refs": ["sg-1"]}]
        assert _grounded(recs, {"sg-1", "vol-2"}) == recs


class TestPrompt:
    def test_prompt_contains_low_categories_and_evidence(self):
        f = PostureFinding("network", "cis-4.1", "sg-1", "SecurityGroup", "0.0.0.0/0:22", "high")
        p = _build_prompt("acct-a", {"network": 50.0, "iam": 100.0}, [f])
        assert "network" in p
        assert "sg-1" in p and "cis-4.1" in p
        assert "iam" not in p.split("Evidence")[0]  # 100-scoring category not listed as low
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_advisor.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'agenticops.security.advisor'`

- [ ] **Step 3: 实现 advisor.py（本 Task 部分）**

```python
"""Evidence-grounded security recommendation advisor — the ONLY LLM component
in the security engine. Fail-closed end to end: ungrounded refs, critic
refutation, or any exception -> the recommendation is dropped, never guessed.
Recommendations never touch scoring (principle 4)."""
from __future__ import annotations

import json
import logging
import re

logger = logging.getLogger(__name__)

_ARRAY_RE = re.compile(r"\[.*\]", re.DOTALL)


def _parse_recommendations(text: str) -> list[dict]:
    """First JSON array in the text (code fences tolerated); invalid -> []."""
    cleaned = re.sub(r"```(?:json)?", "", text or "")
    m = _ARRAY_RE.search(cleaned)
    if not m:
        return []
    try:
        data = json.loads(m.group(0))
    except (json.JSONDecodeError, ValueError):
        return []
    return [r for r in data if isinstance(r, dict)] if isinstance(data, list) else []


def _grounded(recs: list[dict], resource_ids: set[str]) -> list[dict]:
    """Keep only recommendations whose every evidence ref is a resource we
    actually collected this round. Empty evidence -> dropped (must cite)."""
    out = []
    for r in recs:
        refs = r.get("evidence_refs") or []
        if refs and all(str(ref) in resource_ids for ref in refs):
            out.append(r)
        else:
            logger.info("advisor: dropped ungrounded recommendation %r", r.get("title"))
    return out


def _build_prompt(account: str, category_scores: dict, findings: list) -> str:
    low = sorted(c for c, s in category_scores.items() if s < 100.0)
    evidence = "\n".join(
        f"- {f.resource_id} — {f.raw_check} ({f.control_id}, {f.category})"
        for f in findings
    )
    return (
        "You are a cloud security advisor. Based ONLY on the evidence below, "
        f"produce prioritized remediation recommendations for account {account}.\n"
        f"Low-scoring categories: {', '.join(low)}\n"
        f"Evidence (the only resources you may cite):\n{evidence}\n\n"
        "Rules: every recommendation MUST cite resource ids from the evidence in "
        "evidence_refs, verbatim. Do not invent resources. Respond with ONLY a JSON "
        "array: [{\"category\": str, \"title\": str, \"detail\": str, "
        "\"evidence_refs\": [str], \"severity\": \"low|medium|high|critical\", "
        "\"confidence\": 0.0-1.0}]"
    )
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/advisor.py && python -m pytest tests/test_security_advisor.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/advisor.py tests/test_security_advisor.py
git commit --no-verify -m "feat(security): advisor pure parts — parse/ground/prompt (Stage 5)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5.2: recommend() — LLM + critic + 落库（fail-closed）★

**Files:**
- Modify: `src/agenticops/security/advisor.py`
- Test: `tests/test_security_advisor.py`

**Interfaces:**
- Produces:
  - `_critic(rec: dict, evidence: str, model: str) -> str | None`——一次 `_call_bedrock` 对抗审，返回 `supported|weak|refuted`；解析失败/非法 verdict → `None`。
  - `recommend(snapshot_id: int | None, account: str, score_result, findings: list) -> int`——返回落库行数。流程：`security_advisor_enabled` 关 → 0；无低分类别 → 0；模型 = `settings.security_model_id or settings.bedrock_model_id_cheap`；`_call_bedrock`（max_tokens=1500）→ `_parse_recommendations` → `_grounded`（对 `{f.resource_id}` 集合）→ 每条 critic（`security_advisor_critic_enabled` 开时；`refuted` 或 `None` → 丢弃）→ 存活者写 `SecurityRecommendation(snapshot_id, account_id=account, category, title, detail, evidence_refs, severity, critic_verdict, confidence, status="open")`。**任何异常 → WARNING + return 0**。severity 非法值 → "medium"；confidence 非数值 → 0.5，clamp [0,1]。
- Consumes: `signal_gate._call_bedrock/_parse_verdict`、`models.SecurityRecommendation/get_db_session`、`scoring.ScoreResult`。

- [ ] **Step 1: 写失败测试**

```python
from contextlib import contextmanager

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from agenticops import models
from agenticops.security.scoring import score


@pytest.fixture
def sess_factory():
    engine = create_engine("sqlite:///:memory:")
    models.Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)

    @contextmanager
    def _sess():
        s = Session()
        try:
            yield s
            s.commit()
        finally:
            s.close()

    return _sess


def _findings():
    return [PostureFinding("network", "cis-4.1", "sg-1", "SecurityGroup", "0.0.0.0/0:22", "high")]


_LLM_OK = ('[{"category": "network", "title": "Restrict SSH", "detail": "close it", '
           '"evidence_refs": ["sg-1"], "severity": "high", "confidence": 0.9}]')


class TestRecommend:
    def _run(self, sess_factory, llm_side_effect, critic_enabled=True):
        import agenticops.security.advisor as adv
        from unittest.mock import patch
        with patch.object(models, "get_db_session", None, create=True):
            pass
        with patch("agenticops.security.advisor.get_db_session", sess_factory), \
             patch("agenticops.services.signal_gate._call_bedrock",
                   side_effect=llm_side_effect), \
             patch("agenticops.config.settings.security_advisor_critic_enabled",
                   critic_enabled):
            return adv.recommend(None, "acct-a", score(_findings()), _findings())

    def test_grounded_supported_recommendation_persisted(self, sess_factory):
        calls = [(_LLM_OK, {}), ('{"verdict": "supported", "notes": ""}', {})]
        n = self._run(sess_factory, calls)
        assert n == 1
        with sess_factory() as s:
            rec = s.query(models.SecurityRecommendation).one()
            assert rec.title == "Restrict SSH"
            assert rec.critic_verdict == "supported"
            assert rec.evidence_refs == ["sg-1"]
            assert rec.status == "open"

    def test_critic_refuted_dropped(self, sess_factory):
        calls = [(_LLM_OK, {}), ('{"verdict": "refuted", "notes": "wrong"}', {})]
        n = self._run(sess_factory, calls)
        assert n == 0
        with sess_factory() as s:
            assert s.query(models.SecurityRecommendation).count() == 0

    def test_ungrounded_ref_dropped_before_critic(self, sess_factory):
        bad = _LLM_OK.replace("sg-1", "sg-GHOST")
        n = self._run(sess_factory, [(bad, {})])
        assert n == 0

    def test_llm_exception_fail_closed(self, sess_factory):
        n = self._run(sess_factory, RuntimeError("bedrock down"))
        assert n == 0

    def test_critic_disabled_defaults_supported(self, sess_factory):
        n = self._run(sess_factory, [(_LLM_OK, {})], critic_enabled=False)
        assert n == 1
        with sess_factory() as s:
            assert s.query(models.SecurityRecommendation).one().critic_verdict == "supported"
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_advisor.py::TestRecommend -v`
Expected: FAIL — `AttributeError: module ... has no attribute 'recommend'`

- [ ] **Step 3: 实现**（`advisor.py` 追加）

```python
from agenticops.models import SecurityRecommendation, get_db_session

_VALID_SEVERITY = {"low", "medium", "high", "critical"}
_VALID_VERDICTS = {"supported", "weak", "refuted"}


def _critic(rec: dict, evidence: str, model: str) -> str | None:
    """One adversarial cheap-model pass. Returns a valid verdict or None."""
    from agenticops.services.signal_gate import _call_bedrock, _parse_verdict
    prompt = (
        "You are an adversarial reviewer. Given the evidence and a proposed security "
        "recommendation, judge whether the evidence actually supports it.\n"
        f"Evidence:\n{evidence}\n\nRecommendation:\n{json.dumps(rec)}\n\n"
        'Respond with ONLY JSON: {"verdict": "supported" | "weak" | "refuted", "notes": "..."}'
    )
    text, _ = _call_bedrock(prompt, model, max_tokens=300)
    parsed = _parse_verdict(text) or {}
    verdict = str(parsed.get("verdict", "")).lower()
    return verdict if verdict in _VALID_VERDICTS else None


def recommend(snapshot_id, account: str, score_result, findings: list) -> int:
    """Generate + persist grounded recommendations. Fail-closed: 0 rows on any error."""
    from agenticops.config import settings

    if not settings.security_advisor_enabled:
        return 0
    try:
        low = [c for c, s in score_result.category_scores.items() if s < 100.0]
        if not low or not findings:
            return 0
        model = settings.security_model_id or settings.bedrock_model_id_cheap
        from agenticops.services.signal_gate import _call_bedrock
        text, _ = _call_bedrock(_build_prompt(account, score_result.category_scores, findings),
                                model, max_tokens=1500)
        resource_ids = {f.resource_id for f in findings}
        recs = _grounded(_parse_recommendations(text), resource_ids)
        evidence = "\n".join(f"- {f.resource_id} — {f.raw_check} ({f.control_id})"
                             for f in findings)
        surviving: list[tuple[dict, str]] = []
        for r in recs:
            verdict = "supported"
            if settings.security_advisor_critic_enabled:
                verdict = _critic(r, evidence, model)
                if verdict is None or verdict == "refuted":
                    logger.info("advisor: critic dropped %r (verdict=%s)",
                                r.get("title"), verdict)
                    continue
            surviving.append((r, verdict))
        if not surviving:
            return 0
        with get_db_session() as session:
            for r, verdict in surviving:
                sev = str(r.get("severity", "medium")).lower()
                try:
                    conf = max(0.0, min(1.0, float(r.get("confidence", 0.5))))
                except (TypeError, ValueError):
                    conf = 0.5
                session.add(SecurityRecommendation(
                    snapshot_id=snapshot_id, account_id=account,
                    category=str(r.get("category", "other"))[:32],
                    title=str(r.get("title", ""))[:256],
                    detail=str(r.get("detail", "")),
                    evidence_refs=[str(x) for x in (r.get("evidence_refs") or [])],
                    severity=sev if sev in _VALID_SEVERITY else "medium",
                    critic_verdict=verdict, confidence=conf, status="open",
                ))
        return len(surviving)
    except Exception as e:
        logger.warning("advisor failed for %s (fail-closed, no rows): %s", account, e)
        return 0
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/advisor.py && python -m pytest tests/test_security_advisor.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/advisor.py tests/test_security_advisor.py
git commit --no-verify -m "feat(security): advisor recommend() — LLM + critic + fail-closed persist (Stage 5)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5.3: advisor 接入 run_posture_snapshot

**Files:**
- Modify: `src/agenticops/security/posture_snapshot.py`
- Test: `tests/test_security_snapshot.py`（追加）

**Interfaces:**
- 行为增量：快照 `session.add` 后 `session.flush()` 取 `snap.id`；session 提交后调 `advisor.recommend(snapshot_id, account, result, findings)`（其自身 fail-closed，无需再包 try）。advisor 结果不影响 `written` 计数。
- Consumes: Task 5.2 `recommend`。

- [ ] **Step 1: 写失败测试**

```python
class TestAdvisorWiring:
    def test_recommend_called_with_flushed_snapshot_id(self, monkeypatch):
        import agenticops.security.posture_snapshot as ps
        from agenticops import models
        from sqlalchemy import create_engine
        from sqlalchemy.orm import sessionmaker
        from contextlib import contextmanager
        engine = create_engine("sqlite:///:memory:")
        models.Base.metadata.create_all(engine)
        Session = sessionmaker(bind=engine)

        @contextmanager
        def _sess():
            s = Session()
            try:
                yield s
                s.commit()
            finally:
                s.close()

        monkeypatch.setattr(ps, "get_db_session", _sess)
        monkeypatch.setattr(ps, "_resolve_security_accounts", lambda: ["acct-a"])
        f = PostureFinding("iam", "cis-1.10", "alice", "IAMUser", "no mfa", "high")
        monkeypatch.setattr(ps, "collect_posture", lambda a: [f])
        monkeypatch.setattr(ps, "_aggregate_topology", lambda a: ({}, {}, {}, {}))
        monkeypatch.setattr(ps, "_emit_posture_signals", lambda a, ann: 0)
        calls = []
        monkeypatch.setattr(ps, "recommend",
                            lambda sid, acct, res, fnd: calls.append((sid, acct)) or 0,
                            raising=False)
        n = ps.run_posture_snapshot()
        assert n == 1
        assert len(calls) == 1
        sid, acct = calls[0]
        assert acct == "acct-a"
        with _sess() as s:
            assert s.query(models.SecuritySnapshot).one().id == sid
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_snapshot.py::TestAdvisorWiring -v`
Expected: FAIL —（`recommend` 未被调用，`len(calls) == 0`）

- [ ] **Step 3: 实现**

`posture_snapshot.py` 顶部 import 区补：

```python
from agenticops.security.advisor import recommend
```

`run_posture_snapshot` 的落库块改为（flush 取 id，session 退出后调 advisor）：

```python
            snapshot_id = None
            with get_db_session() as session:
                snap = SecuritySnapshot(
                    account_id=account, provider="aws",
                    overall_score=result.overall_score,
                    category_scores=result.category_scores,
                    metrics=result.metrics,
                    exposure_paths=_build_exposure_paths(annotated),
                    cis_results=result.cis_results,
                )
                session.add(snap)
                session.flush()
                snapshot_id = snap.id
                _prune_old_snapshots(session)
            _emit_posture_signals(account, annotated)
            recommend(snapshot_id, account, result, findings)  # fail-closed inside
            written += 1
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/security/posture_snapshot.py && python -m pytest tests/test_security_snapshot.py tests/test_security_advisor.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/security/posture_snapshot.py tests/test_security_snapshot.py
git commit --no-verify -m "feat(security): wire advisor into posture snapshot (Stage 5)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

> **阶段 5 验证锚点：** 建议入 `SecurityRecommendation`（带 snapshot_id/critic_verdict/confidence）；LLM 异常时快照与评分照常。

---

## Stage 6 — Security API + 前端 + `security-review` 报告

> **可验证产出：** `/api/security/*` 5 端点返回契约；`/app/security` 页可视；Dashboard 高亮卡；`security-review` 报告可生成。

### Task 6.1: security_service 查询层

**Files:**
- Create: `src/agenticops/services/security_service.py`
- Test: `tests/test_security_api.py`

**Interfaces:**
- Produces（全部返回 plain dict/list，供 router 直接透传）：
  - `security_summary() -> dict`：`{"accounts": [{"account_id", "overall_score", "category_scores", "created_at", "reachable_paths", "open_findings"}], "generated_at"}`——每账户取最新快照；`reachable_paths` = 该快照 exposure_paths 中 `reachability=="reachable"` 的行数；`open_findings` = `HealthIssue.detected_by ∈ ("security_poll","security_posture")` 且 `status != "resolved"` 的数量。
  - `security_trend(days: int = 30, account: str | None = None) -> list[dict]`：`[{"account_id", "created_at", "overall_score"}]` 升序。
  - `security_findings(limit: int = 100) -> list[dict]`：security 来源的 HealthIssue（`detected_by ∈ ("security_poll","security_posture")`），按 `last_seen` 降序：`{"id", "title", "severity", "status", "resource_id", "issue_type", "detected_by", "last_seen", "reachability"}`（reachability 取 `metric_data.get("reachability")`）。
  - `security_recommendations(status: str | None = None, limit: int = 100) -> list[dict]`：`{"id", "account_id", "category", "title", "detail", "severity", "critic_verdict", "confidence", "status", "created_at"}`。
  - `attack_paths() -> list[dict]`：各账户最新快照的 exposure_paths 平铺 + `account_id`。
- Consumes: `models.SecuritySnapshot/SecurityRecommendation/HealthIssue/get_db_session`（模块顶部 import `get_db_session`，便于测试 monkeypatch）。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_security_api.py
"""Stage 6: security service queries + /api/security/* contract."""
from contextlib import contextmanager
from datetime import datetime, timedelta, timezone
from unittest.mock import patch

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from agenticops import models


@pytest.fixture
def sess_factory():
    engine = create_engine("sqlite:///:memory:")
    models.Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)

    @contextmanager
    def _sess():
        s = Session()
        try:
            yield s
            s.commit()
        finally:
            s.close()

    return _sess


def _seed(sess_factory):
    old = datetime.now(timezone.utc) - timedelta(hours=2)
    with sess_factory() as s:
        s.add(models.SecuritySnapshot(
            account_id="acct-a", provider="aws", overall_score=60.0,
            created_at=old, category_scores={"iam": 50.0}, exposure_paths=[]))
        s.add(models.SecuritySnapshot(
            account_id="acct-a", provider="aws", overall_score=75.0,
            category_scores={"iam": 66.6},
            exposure_paths=[{"resource_id": "sg-1", "port": 22,
                             "path": ["internet", "sn-1", "i-1:22"],
                             "reachability": "reachable"}]))
        s.add(models.HealthIssue(
            resource_id="sg-1", severity="high", source="security_posture",
            title="[cis-4.1] open ssh", description="", status="open",
            detected_by="security_posture", issue_type="security_exposure",
            metric_data={"reachability": "reachable"}))
        s.add(models.SecurityRecommendation(
            account_id="acct-a", category="network", title="Restrict SSH",
            detail="close it", evidence_refs=["sg-1"], severity="high",
            critic_verdict="supported", confidence=0.9, status="open"))


class TestSecurityService:
    def test_summary_latest_snapshot_and_counts(self, sess_factory, monkeypatch):
        from agenticops.services import security_service as svc
        monkeypatch.setattr(svc, "get_db_session", sess_factory)
        _seed(sess_factory)
        out = svc.security_summary()
        assert len(out["accounts"]) == 1
        acct = out["accounts"][0]
        assert acct["overall_score"] == 75.0  # latest, not the 60.0 one
        assert acct["reachable_paths"] == 1
        assert acct["open_findings"] == 1

    def test_trend_ascending(self, sess_factory, monkeypatch):
        from agenticops.services import security_service as svc
        monkeypatch.setattr(svc, "get_db_session", sess_factory)
        _seed(sess_factory)
        pts = svc.security_trend(days=30, account="acct-a")
        assert [p["overall_score"] for p in pts] == [60.0, 75.0]

    def test_findings_carry_reachability(self, sess_factory, monkeypatch):
        from agenticops.services import security_service as svc
        monkeypatch.setattr(svc, "get_db_session", sess_factory)
        _seed(sess_factory)
        rows = svc.security_findings()
        assert rows[0]["reachability"] == "reachable"
        assert rows[0]["detected_by"] == "security_posture"

    def test_recommendations_filter_by_status(self, sess_factory, monkeypatch):
        from agenticops.services import security_service as svc
        monkeypatch.setattr(svc, "get_db_session", sess_factory)
        _seed(sess_factory)
        assert len(svc.security_recommendations(status="open")) == 1
        assert svc.security_recommendations(status="dismissed") == []

    def test_attack_paths_flattened_with_account(self, sess_factory, monkeypatch):
        from agenticops.services import security_service as svc
        monkeypatch.setattr(svc, "get_db_session", sess_factory)
        _seed(sess_factory)
        paths = svc.attack_paths()
        assert paths == [{"account_id": "acct-a", "resource_id": "sg-1", "port": 22,
                          "path": ["internet", "sn-1", "i-1:22"],
                          "reachability": "reachable"}]
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_api.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'agenticops.services.security_service'`

- [ ] **Step 3: 实现 security_service.py**

```python
"""Security review query layer for /api/security/* (mirrors cost_service's role)."""
from __future__ import annotations

from datetime import datetime, timedelta, timezone

from agenticops.models import (
    HealthIssue,
    SecurityRecommendation,
    SecuritySnapshot,
    get_db_session,
)

_SECURITY_DETECTORS = ("security_poll", "security_posture")


def _latest_snapshots(session) -> list[SecuritySnapshot]:
    """Newest snapshot per account."""
    snaps: dict[str, SecuritySnapshot] = {}
    for snap in session.query(SecuritySnapshot).order_by(SecuritySnapshot.created_at.asc()):
        snaps[snap.account_id] = snap  # later rows overwrite -> newest wins
    return list(snaps.values())


def security_summary() -> dict:
    with get_db_session() as session:
        open_counts: dict = {}
        rows = (session.query(HealthIssue)
                .filter(HealthIssue.detected_by.in_(_SECURITY_DETECTORS))
                .filter(HealthIssue.status != "resolved").all())
        total_open = len(rows)
        accounts = []
        for snap in _latest_snapshots(session):
            reachable = sum(1 for p in (snap.exposure_paths or [])
                            if p.get("reachability") == "reachable")
            accounts.append({
                "account_id": snap.account_id,
                "overall_score": snap.overall_score,
                "category_scores": snap.category_scores or {},
                "created_at": snap.created_at.isoformat() if snap.created_at else None,
                "reachable_paths": reachable,
                "open_findings": total_open,
            })
        return {"accounts": accounts,
                "generated_at": datetime.now(timezone.utc).isoformat()}


def security_trend(days: int = 30, account: str | None = None) -> list[dict]:
    cutoff = datetime.now(timezone.utc) - timedelta(days=max(1, int(days)))
    with get_db_session() as session:
        q = (session.query(SecuritySnapshot)
             .filter(SecuritySnapshot.created_at >= cutoff)
             .order_by(SecuritySnapshot.created_at.asc()))
        if account:
            q = q.filter(SecuritySnapshot.account_id == account)
        return [{"account_id": s.account_id,
                 "created_at": s.created_at.isoformat() if s.created_at else None,
                 "overall_score": s.overall_score} for s in q]


def security_findings(limit: int = 100) -> list[dict]:
    with get_db_session() as session:
        q = (session.query(HealthIssue)
             .filter(HealthIssue.detected_by.in_(_SECURITY_DETECTORS))
             .order_by(HealthIssue.last_seen.desc()).limit(max(1, int(limit))))
        return [{"id": i.id, "title": i.title, "severity": i.severity,
                 "status": i.status, "resource_id": i.resource_id,
                 "issue_type": i.issue_type, "detected_by": i.detected_by,
                 "last_seen": i.last_seen.isoformat() if i.last_seen else None,
                 "reachability": (i.metric_data or {}).get("reachability")}
                for i in q]


def security_recommendations(status: str | None = None, limit: int = 100) -> list[dict]:
    with get_db_session() as session:
        q = (session.query(SecurityRecommendation)
             .order_by(SecurityRecommendation.created_at.desc())
             .limit(max(1, int(limit))))
        if status:
            q = (session.query(SecurityRecommendation)
                 .filter(SecurityRecommendation.status == status)
                 .order_by(SecurityRecommendation.created_at.desc())
                 .limit(max(1, int(limit))))
        return [{"id": r.id, "account_id": r.account_id, "category": r.category,
                 "title": r.title, "detail": r.detail, "severity": r.severity,
                 "critic_verdict": r.critic_verdict, "confidence": r.confidence,
                 "status": r.status,
                 "created_at": r.created_at.isoformat() if r.created_at else None}
                for r in q]


def attack_paths() -> list[dict]:
    with get_db_session() as session:
        out: list[dict] = []
        for snap in _latest_snapshots(session):
            for p in snap.exposure_paths or []:
                out.append({"account_id": snap.account_id, **p})
        return out
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/services/security_service.py && python -m pytest tests/test_security_api.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/services/security_service.py tests/test_security_api.py
git commit --no-verify -m "feat(security): security_service query layer (Stage 6)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6.2: /api/security router + app 接入 + pipeline 下拉

**Files:**
- Create: `src/agenticops/web/routers/security.py`
- Modify: `src/agenticops/web/app.py`（signals router include 之后 ~line 311；pipeline-options 列表 line 3554）
- Test: `tests/test_security_api.py`（追加）

**Interfaces:**
- Produces: `GET /api/security/summary`、`GET /api/security/trend?days=&account=`、`GET /api/security/findings?limit=`、`GET /api/security/recommendations?status=&limit=`、`GET /api/security/attack-paths`——全部薄透传 `security_service`（handler 内 lazy import，镜像 cost.py）。
- pipeline-options `"pipelines"` 列表追加 `"SecurityPostureSnapshot", "SecurityIncrementalPoll"`。

- [ ] **Step 1: 写失败测试**（追加）

```python
from fastapi.testclient import TestClient

from agenticops.web.app import app

client = TestClient(app)


class TestSecurityApiContract:
    def test_summary_endpoint(self):
        fake = {"accounts": [], "generated_at": "2026-08-31T00:00:00+00:00"}
        with patch("agenticops.services.security_service.security_summary", return_value=fake):
            r = client.get("/api/security/summary")
        assert r.status_code == 200
        assert r.json() == fake

    def test_trend_params_forwarded(self):
        with patch("agenticops.services.security_service.security_trend",
                   return_value=[]) as m:
            r = client.get("/api/security/trend?days=7&account=acct-a")
        assert r.status_code == 200
        assert m.call_args.kwargs == {"days": 7, "account": "acct-a"}

    def test_findings_recommendations_attack_paths(self):
        with patch("agenticops.services.security_service.security_findings",
                   return_value=[]), \
             patch("agenticops.services.security_service.security_recommendations",
                   return_value=[]), \
             patch("agenticops.services.security_service.attack_paths",
                   return_value=[]):
            assert client.get("/api/security/findings").status_code == 200
            assert client.get("/api/security/recommendations?status=open").status_code == 200
            assert client.get("/api/security/attack-paths").status_code == 200

    def test_pipeline_options_include_security_jobs(self):
        r = client.get("/api/schedules/pipeline-options")
        assert r.status_code == 200
        pipes = r.json()["pipelines"]
        assert "SecurityPostureSnapshot" in pipes
        assert "SecurityIncrementalPoll" in pipes
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_api.py::TestSecurityApiContract -v`
Expected: FAIL — 404（router 未注册）

- [ ] **Step 3: 实现**

`web/routers/security.py`：

```python
"""Security review API — posture scores, findings, recommendations, attack paths."""

from typing import Optional

from fastapi import APIRouter, Query

router = APIRouter()


@router.get("/api/security/summary")
async def api_security_summary():
    from agenticops.services import security_service
    return security_service.security_summary()


@router.get("/api/security/trend")
async def api_security_trend(days: int = Query(30), account: Optional[str] = None):
    from agenticops.services import security_service
    return security_service.security_trend(days=days, account=account)


@router.get("/api/security/findings")
async def api_security_findings(limit: int = Query(100)):
    from agenticops.services import security_service
    return security_service.security_findings(limit=limit)


@router.get("/api/security/recommendations")
async def api_security_recommendations(status: Optional[str] = None, limit: int = Query(100)):
    from agenticops.services import security_service
    return security_service.security_recommendations(status=status, limit=limit)


@router.get("/api/security/attack-paths")
async def api_security_attack_paths():
    from agenticops.services import security_service
    return security_service.attack_paths()
```

`app.py` signals router include 之后加两行（镜像既有模式）：

```python
from agenticops.web.routers import security as _security_router
app.include_router(_security_router.router)
```

`app.py:3554` pipelines 列表改为：

```python
        "pipelines": ["FullScan", "Monitoring", "DailyReport", "HealthPatrol", "GalaxyBuild",
                      "SecurityPostureSnapshot", "SecurityIncrementalPoll", "AgentChain"],
```

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/web/app.py src/agenticops/web/routers/security.py && python -m pytest tests/test_security_api.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/web/routers/security.py src/agenticops/web/app.py tests/test_security_api.py
git commit --no-verify -m "feat(security): /api/security router + pipeline options (Stage 6)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6.3: `security-review` 报告类型（6 处编辑）

**Files:**
- Modify: `src/agenticops/report/generator.py`（`generate_network_health_report` 之后加方法）
- Modify: `src/agenticops/web/app.py`（report dispatch elif，~line 3218）
- Modify: `src/agenticops/web/schemas.py:172`（正则）
- Modify: `src/agenticops/tools/report_tools.py:41`（valid_types）
- Modify: `src/agenticops/agents/reporter_agent.py`（prompt bullet + docstring line 114）
- Modify: `src/agenticops/web/frontend/src/pages/Reports.tsx:10`（TYPE_COLORS）
- Test: `tests/test_security_report.py`

**Interfaces:**
- Produces: `ReportGenerator.generate_security_review_report(self, save: bool = True) -> str`——markdown：header + 最新快照评分表（overall + 分类）+ CIS 结果表 + 暴露链列表 + open 建议列表；`self.account` 存在则按 `self.account.name` 过滤，否则全账户；`save=True` 时 `self._save_report(report_type="security-review", title=f"Security Review - {YYYY-MM-DD}", content=md)`。无快照 → 返回含 "No security snapshot available" 的 md（照常可保存）。
- schemas 正则改为 `^(daily|inventory|anomaly|newsletter|conversation|incident|security-review)$`；report_tools `valid_types` 加 `"security-review"`；reporter_agent GATHER DATA 列表加 `**security-review**: latest SecuritySnapshot scores + exposure paths + open recommendations (use generate_report tool)`，docstring 类型列表同步；Reports.tsx `TYPE_COLORS` 加 `"security-review": "bg-rose-100 text-rose-700"`。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_security_report.py
"""Stage 6: security-review report generation."""
from contextlib import contextmanager
from unittest.mock import patch

import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from agenticops import models


@pytest.fixture
def sess_factory():
    engine = create_engine("sqlite:///:memory:")
    models.Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)

    @contextmanager
    def _sess():
        s = Session()
        try:
            yield s
            s.commit()
        finally:
            s.close()

    return _sess


class TestSecurityReviewReport:
    def test_markdown_contains_scores_and_paths(self, sess_factory):
        with sess_factory() as s:
            s.add(models.SecuritySnapshot(
                account_id="acct-a", provider="aws", overall_score=62.5,
                category_scores={"iam": 33.3, "network": 50.0},
                cis_results={"cis-1.10": "fail", "cis-4.1": "fail"},
                exposure_paths=[{"resource_id": "sg-1", "port": 22,
                                 "path": ["internet", "sn-1", "i-1:22"],
                                 "reachability": "reachable"}]))
            s.add(models.SecurityRecommendation(
                account_id="acct-a", category="network", title="Restrict SSH",
                detail="close 0.0.0.0/0:22", evidence_refs=["sg-1"], severity="high",
                critic_verdict="supported", confidence=0.9, status="open"))
        from agenticops.report.generator import ReportGenerator
        gen = ReportGenerator()
        with patch("agenticops.report.generator.get_db_session", sess_factory, create=True):
            md = gen.generate_security_review_report(save=False)
        assert "# Security Review Report" in md
        assert "62.5" in md
        assert "cis-4.1" in md
        assert "sg-1" in md
        assert "Restrict SSH" in md

    def test_no_snapshot_graceful(self, sess_factory):
        from agenticops.report.generator import ReportGenerator
        gen = ReportGenerator()
        with patch("agenticops.report.generator.get_db_session", sess_factory, create=True):
            md = gen.generate_security_review_report(save=False)
        assert "No security snapshot available" in md

    def test_schema_accepts_type(self):
        from agenticops.web.schemas import ReportGenerateRequest
        req = ReportGenerateRequest(report_type="security-review")
        assert req.report_type == "security-review"

    def test_report_tools_accept_type(self):
        import inspect
        from agenticops.tools import report_tools
        src = inspect.getsource(report_tools)
        assert "security-review" in src
```

- [ ] **Step 2: 跑红**

Run: `python -m pytest tests/test_security_report.py -v`
Expected: FAIL — `AttributeError: 'ReportGenerator' object has no attribute 'generate_security_review_report'`

- [ ] **Step 3: 实现**

`generator.py`（`generate_network_health_report` 之后加；文件顶部确认/补 `from agenticops.models import get_db_session` —— 若既有代码用其他 session 入口，跟随既有模式并同步调整测试的 patch 目标）：

```python
    def generate_security_review_report(self, save: bool = True) -> str:
        """Security review: latest posture scores + CIS results + exposure paths
        + open recommendations. Deterministic — reads persisted snapshots only."""
        from agenticops.models import SecurityRecommendation, SecuritySnapshot

        now = datetime.now(timezone.utc)
        lines = [
            "# Security Review Report",
            f"**Generated**: {now.strftime('%Y-%m-%d %H:%M UTC')}",
            "",
            "---",
            "",
        ]
        with get_db_session() as session:
            q = session.query(SecuritySnapshot).order_by(SecuritySnapshot.created_at.asc())
            if self.account:
                q = q.filter(SecuritySnapshot.account_id == self.account.name)
            latest: dict[str, SecuritySnapshot] = {}
            for snap in q:
                latest[snap.account_id] = snap
            if not latest:
                lines.append("No security snapshot available yet. "
                             "Run the SecurityPostureSnapshot schedule first.")
            for account_id, snap in sorted(latest.items()):
                lines += [f"## Account: {account_id}",
                          f"**Overall score**: {snap.overall_score:.1f} / 100", "",
                          "| Category | Score |", "|---|---|"]
                for cat, val in sorted((snap.category_scores or {}).items()):
                    lines.append(f"| {cat} | {val:.1f} |")
                lines += ["", "| CIS Control | Result |", "|---|---|"]
                for cid, res in sorted((snap.cis_results or {}).items()):
                    lines.append(f"| {cid} | {res} |")
                paths = snap.exposure_paths or []
                if paths:
                    lines += ["", "### Exposure paths"]
                    for p in paths:
                        chain = " -> ".join(str(x) for x in (p.get("path") or []))
                        lines.append(f"- **{p.get('reachability')}** {p.get('resource_id')}"
                                     f" port {p.get('port')}: {chain or 'n/a'}")
                lines.append("")
            recs = (session.query(SecurityRecommendation)
                    .filter(SecurityRecommendation.status == "open")
                    .order_by(SecurityRecommendation.created_at.desc()).limit(20).all())
            if recs:
                lines += ["## Open Recommendations", ""]
                for r in recs:
                    lines.append(f"- [{r.severity}] **{r.title}** ({r.category}, "
                                 f"critic={r.critic_verdict}, conf={r.confidence:.1f}): {r.detail}")
        lines += ["", "*Report generated by AgenticOps*"]
        report_md = "\n".join(lines)
        if save:
            self._save_report(
                report_type="security-review",
                title=f"Security Review - {now.strftime('%Y-%m-%d')}",
                content=report_md,
            )
        return report_md
```

其余 5 处编辑：
1. `app.py` dispatch（`elif request.report_type == "inventory":` 分支后）：
```python
            elif request.report_type == "security-review":
                generator.generate_security_review_report()
```
2. `schemas.py:172` 正则：`pattern="^(daily|inventory|anomaly|newsletter|conversation|incident|security-review)$"`
3. `report_tools.py:41`：`valid_types = {"daily", "incident", "inventory", "network", "newsletter", "security-review"}`
4. `reporter_agent.py` GATHER DATA 列表（`**inventory**` bullet 后）加一行：`   **security-review**: Latest SecuritySnapshot scores, CIS results, exposure paths, open recommendations — use the generate_report tool with report_type="security-review"`；line 114 docstring 改为 `report_type: Type of report: daily, incident, inventory, or security-review`
5. `Reports.tsx` TYPE_COLORS 加：`"security-review": "bg-rose-100 text-rose-700",`

- [ ] **Step 4: 跑绿**

Run: `python3 -m py_compile src/agenticops/report/generator.py src/agenticops/web/app.py src/agenticops/web/schemas.py src/agenticops/tools/report_tools.py src/agenticops/agents/reporter_agent.py && python -m pytest tests/test_security_report.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add src/agenticops/report/generator.py src/agenticops/web/app.py src/agenticops/web/schemas.py \
        src/agenticops/tools/report_tools.py src/agenticops/agents/reporter_agent.py \
        src/agenticops/web/frontend/src/pages/Reports.tsx tests/test_security_report.py
git commit --no-verify -m "feat(security): security-review report type across 6 edit points (Stage 6)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6.4: 前端数据层（types + useSecurity hooks）

**Files:**
- Modify: `src/agenticops/web/frontend/src/api/types.ts`（末尾 `GalaxyGraph` 之后追加）
- Create: `src/agenticops/web/frontend/src/hooks/useSecurity.ts`
- Verify: `cd src/agenticops/web/frontend && npx tsc --noEmit`

**Interfaces:**
- Produces（types.ts）：

```ts
// ── Security review (MVP-2.5.0) ────────────────────────────────
export interface SecurityAccountScore {
  account_id: string;
  overall_score: number;
  category_scores: Record<string, number>;
  created_at: string | null;
  reachable_paths: number;
  open_findings: number;
}

export interface SecuritySummary {
  accounts: SecurityAccountScore[];
  generated_at: string;
}

export interface SecurityTrendPoint {
  account_id: string;
  created_at: string | null;
  overall_score: number;
}

export interface SecurityFindingItem {
  id: number;
  title: string;
  severity: string;
  status: string;
  resource_id: string;
  issue_type: string;
  detected_by: string;
  last_seen: string | null;
  reachability: string | null;
}

export interface SecurityRecommendationItem {
  id: number;
  account_id: string;
  category: string;
  title: string;
  detail: string;
  severity: string;
  critic_verdict: string;
  confidence: number;
  status: string;
  created_at: string | null;
}

export interface AttackPathItem {
  account_id: string;
  resource_id: string;
  port: number | null;
  path: string[];
  reachability: string;
}
```

- Produces（useSecurity.ts，镜像 useGalaxy.ts；`apiFetch` 自动加 `/api` 前缀）：

```ts
import { useQuery } from "@tanstack/react-query";
import { apiFetch } from "@/api/client";
import type {
  AttackPathItem,
  SecurityFindingItem,
  SecurityRecommendationItem,
  SecuritySummary,
  SecurityTrendPoint,
} from "@/api/types";

export function useSecuritySummary() {
  return useQuery({
    queryKey: ["security-summary"],
    queryFn: () => apiFetch<SecuritySummary>("/security/summary"),
    staleTime: 30_000,
  });
}

export function useSecurityTrend(days = 30, account?: string) {
  const qs = account ? `?days=${days}&account=${encodeURIComponent(account)}` : `?days=${days}`;
  return useQuery({
    queryKey: ["security-trend", days, account ?? ""],
    queryFn: () => apiFetch<SecurityTrendPoint[]>(`/security/trend${qs}`),
    staleTime: 30_000,
  });
}

export function useSecurityFindings(limit = 100) {
  return useQuery({
    queryKey: ["security-findings", limit],
    queryFn: () => apiFetch<SecurityFindingItem[]>(`/security/findings?limit=${limit}`),
    staleTime: 30_000,
  });
}

export function useSecurityRecommendations(status = "open") {
  return useQuery({
    queryKey: ["security-recommendations", status],
    queryFn: () =>
      apiFetch<SecurityRecommendationItem[]>(`/security/recommendations?status=${status}`),
    staleTime: 30_000,
  });
}

export function useAttackPaths() {
  return useQuery({
    queryKey: ["security-attack-paths"],
    queryFn: () => apiFetch<AttackPathItem[]>("/security/attack-paths"),
    staleTime: 30_000,
  });
}
```

- [ ] **Step 1: 写入上述两个文件（本任务无后端测试，验收 = tsc）**
- [ ] **Step 2: 验证**

Run: `cd src/agenticops/web/frontend && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 3: 提交**

```bash
git add src/agenticops/web/frontend/src/api/types.ts src/agenticops/web/frontend/src/hooks/useSecurity.ts
git commit --no-verify -m "feat(security): frontend types + useSecurity hooks (Stage 6)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6.5: Security 页 + 路由 + 导航 + i18n

**Files:**
- Create: `src/agenticops/web/frontend/src/pages/Security.tsx`
- Modify: `src/agenticops/web/frontend/src/App.tsx`（lazy import 区末尾 + galaxy route 之后）
- Modify: `src/agenticops/web/frontend/src/components/layout/NavItems.tsx`（NAV_ITEMS + ICON_PATHS）
- Modify: `src/agenticops/web/frontend/src/locales/en.json`、`zh.json`

**Interfaces:**
- Produces: `/app/security` 页：每账户评分卡（overall + reachable/open 计数）、30 天趋势线（recharts `ComposedChart`+`Line`，项目既定模式）、类别雷达（`RadarChart`/`Radar`/`PolarGrid`/`PolarAngleAxis`，recharts 3 自带）、发现列表（severity 用 Dashboard 同款 `SEV_DOT` 色点 + reachability 徽标）、建议列表（critic_verdict + confidence）、暴露链列表（`internet -> subnet -> instance:port` 等宽字体）。全部用 `Card/CardHeader/CardBody`（`@/components/ui/Card`）。
- 页面为整页路由，无 overlay/side-panel（popup house rule 不触发）。
- Nav：`{ id: "security", to: "/app/security", icon: "shield", labelKey: "nav.security", end: false }`；`ICON_PATHS` 加 `shield: "M9 12.75 11.25 15 15 9.75m-3-7.036A11.959 11.959 0 0 1 3.598 6 11.99 11.99 0 0 0 3 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285Z"`（heroicons shield-check outline）。
- locales：`en.json` 加 `"nav.security": "Security"`；`zh.json` 加 `"nav.security": "安全"`（放在 `nav.galaxy` 行后）。

- [ ] **Step 1: 实现 Security.tsx**（骨架如下，样式跟随 Dashboard/AgentMetrics 既有 token：`bg-card`、`text-muted-foreground`、`font-mono`）

```tsx
import { useTranslation } from "react-i18next";
import {
  ResponsiveContainer, ComposedChart, Line, XAxis, YAxis, Tooltip,
  RadarChart, Radar, PolarGrid, PolarAngleAxis,
} from "recharts";
import { Card, CardHeader, CardBody } from "@/components/ui/Card";
import { Spinner } from "@/components/ui/Spinner";
import {
  useAttackPaths, useSecurityFindings, useSecurityRecommendations,
  useSecuritySummary, useSecurityTrend,
} from "@/hooks/useSecurity";

const SEV_DOT: Record<string, string> = {
  critical: "bg-red-500",
  high: "bg-orange-500",
  medium: "bg-amber-400",
  low: "bg-blue-500 dark:bg-green-500",
};

const REACH_BADGE: Record<string, string> = {
  reachable: "bg-red-100 text-red-700",
  undetermined: "bg-amber-100 text-amber-700",
  not_reachable: "bg-emerald-100 text-emerald-700",
};

export default function Security() {
  const { t } = useTranslation();
  const summary = useSecuritySummary();
  const trend = useSecurityTrend(30);
  const findings = useSecurityFindings(50);
  const recs = useSecurityRecommendations("open");
  const paths = useAttackPaths();

  if (summary.isLoading) return <Spinner />;
  const accounts = summary.data?.accounts ?? [];
  const radarData = accounts[0]
    ? Object.entries(accounts[0].category_scores).map(([category, score]) => ({ category, score }))
    : [];

  return (
    <div className="p-6 space-y-6">
      <h1 className="text-xl font-medium">{t("nav.security")}</h1>

      {/* score cards */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {accounts.map((a) => (
          <Card key={a.account_id}>
            <CardBody>
              <div className="text-sm text-muted-foreground">{a.account_id}</div>
              <div className={`text-3xl font-light font-mono ${a.overall_score < 70 ? "text-primary" : ""}`}>
                {a.overall_score.toFixed(1)}
              </div>
              <div className="text-xs text-muted-foreground mt-1">
                {a.reachable_paths} reachable · {a.open_findings} open
              </div>
            </CardBody>
          </Card>
        ))}
        {accounts.length === 0 && (
          <div className="text-sm text-muted-foreground">No security snapshot yet.</div>
        )}
      </div>

      {/* trend + radar */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <Card>
          <CardHeader>Score trend (30d)</CardHeader>
          <CardBody className="h-64">
            <ResponsiveContainer width="100%" height="100%">
              <ComposedChart data={trend.data ?? []}>
                <XAxis dataKey="created_at" hide />
                <YAxis domain={[0, 100]} width={30} />
                <Tooltip />
                <Line type="monotone" dataKey="overall_score" dot={false} strokeWidth={2} />
              </ComposedChart>
            </ResponsiveContainer>
          </CardBody>
        </Card>
        <Card>
          <CardHeader>Category scores</CardHeader>
          <CardBody className="h-64">
            <ResponsiveContainer width="100%" height="100%">
              <RadarChart data={radarData}>
                <PolarGrid />
                <PolarAngleAxis dataKey="category" />
                <Radar dataKey="score" fillOpacity={0.4} />
              </RadarChart>
            </ResponsiveContainer>
          </CardBody>
        </Card>
      </div>

      {/* findings */}
      <Card>
        <CardHeader>Findings</CardHeader>
        <CardBody>
          {(findings.data ?? []).map((f) => (
            <div key={f.id} className="flex items-center gap-2 py-1.5 text-sm border-b border-border/50 last:border-0">
              <span className={`block w-2 h-2 rounded-full ${SEV_DOT[f.severity] ?? "bg-muted-foreground"}`} />
              <span className="flex-1 truncate">{f.title}</span>
              {f.reachability && (
                <span className={`text-xs px-1.5 py-0.5 rounded ${REACH_BADGE[f.reachability] ?? ""}`}>
                  {f.reachability}
                </span>
              )}
              <span className="text-xs text-muted-foreground">{f.status}</span>
            </div>
          ))}
          {(findings.data ?? []).length === 0 && (
            <div className="text-sm text-muted-foreground">No security findings.</div>
          )}
        </CardBody>
      </Card>

      {/* recommendations */}
      <Card>
        <CardHeader>Recommendations</CardHeader>
        <CardBody>
          {(recs.data ?? []).map((r) => (
            <div key={r.id} className="py-1.5 text-sm border-b border-border/50 last:border-0">
              <div className="flex items-center gap-2">
                <span className={`block w-2 h-2 rounded-full ${SEV_DOT[r.severity] ?? "bg-muted-foreground"}`} />
                <span className="font-medium">{r.title}</span>
                <span className="text-xs text-muted-foreground">
                  {r.critic_verdict} · {(r.confidence * 100).toFixed(0)}%
                </span>
              </div>
              <div className="text-xs text-muted-foreground ml-4">{r.detail}</div>
            </div>
          ))}
          {(recs.data ?? []).length === 0 && (
            <div className="text-sm text-muted-foreground">No open recommendations.</div>
          )}
        </CardBody>
      </Card>

      {/* attack paths */}
      <Card>
        <CardHeader>Exposure paths</CardHeader>
        <CardBody>
          {(paths.data ?? []).map((p, i) => (
            <div key={i} className="flex items-center gap-2 py-1.5 text-sm font-mono border-b border-border/50 last:border-0">
              <span className={`text-xs px-1.5 py-0.5 rounded ${REACH_BADGE[p.reachability] ?? ""}`}>
                {p.reachability}
              </span>
              <span className="truncate">{(p.path ?? []).join(" → ") || p.resource_id}</span>
              <span className="text-xs text-muted-foreground">:{p.port}</span>
            </div>
          ))}
          {(paths.data ?? []).length === 0 && (
            <div className="text-sm text-muted-foreground">No exposure paths.</div>
          )}
        </CardBody>
      </Card>
    </div>
  );
}
```

> 注：`Spinner` 的实际导出路径以 `App.tsx` 既有用法为准（route fallback 已用 `<Spinner />`，同一来源 import）。`Card` 组件的确切子组件名以 `AgentMetrics.tsx` 的 import 为准，不一致就跟随现状调整。

- [ ] **Step 2: 路由 + 导航 + i18n**

`App.tsx`：lazy import 区加 `const Security = lazy(() => import("@/pages/Security"));`；galaxy route 之后加：

```tsx
<Route
  path="security"
  element={
    <Suspense fallback={<Spinner />}>
      <Security />
    </Suspense>
  }
/>
```

`NavItems.tsx`：`NAV_ITEMS` galaxy 项后加 security 项；`ICON_PATHS` 加 `shield`（见 Interfaces）。
`locales/en.json` / `zh.json`：加 `nav.security`。

- [ ] **Step 3: 验证**

Run: `cd src/agenticops/web/frontend && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 4: 提交**

```bash
git add src/agenticops/web/frontend/src/pages/Security.tsx src/agenticops/web/frontend/src/App.tsx \
        src/agenticops/web/frontend/src/components/layout/NavItems.tsx \
        src/agenticops/web/frontend/src/locales/en.json src/agenticops/web/frontend/src/locales/zh.json
git commit --no-verify -m "feat(security): Security page + route + nav + i18n (Stage 6)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6.6: Dashboard 安全高亮卡 + 前端构建收口

**Files:**
- Modify: `src/agenticops/web/frontend/src/pages/Dashboard.tsx`（`<InteractionStats />`（line 84）与 `{/* 4. Row 1 */}`（line 86）之间）
- Modify: `locales/en.json`、`zh.json`（`dashboard.securityScore` / `dashboard.securityReachable`）

**Interfaces:**
- Produces: 有快照数据时显示一条紧凑高亮行：最低账户分 + reachable 暴露数，`score < 70 || reachable > 0` 时用 `text-primary` 高亮；点击导航 `/app/security`；无数据时整块不渲染（不打扰现有布局）。

- [ ] **Step 1: 实现**

`Dashboard.tsx` 顶部加 `import { useSecuritySummary } from "@/hooks/useSecurity";`，组件体内（`stats` 附近）加 `const security = useSecuritySummary();`，插入块：

```tsx
      {/* 3.5 Security highlight (MVP-2.5.0) */}
      {(() => {
        const accounts = security.data?.accounts ?? [];
        if (accounts.length === 0) return null;
        const minScore = Math.min(...accounts.map((a) => a.overall_score));
        const reachable = accounts.reduce((n, a) => n + a.reachable_paths, 0);
        const hot = minScore < 70 || reachable > 0;
        return (
          <button
            onClick={() => navigate("/app/security")}
            className="w-full text-left bg-card border border-border rounded-lg px-4 py-3 mb-6 flex items-center gap-4 hover:bg-accent/50 transition-colors"
          >
            <span className="text-sm text-muted-foreground">{t("dashboard.securityScore")}</span>
            <span className={`text-2xl font-light font-mono ${hot ? "text-primary" : ""}`}>
              {minScore.toFixed(1)}
            </span>
            <span className="text-sm text-muted-foreground ml-auto">
              {reachable} {t("dashboard.securityReachable")}
            </span>
          </button>
        );
      })()}
```

locales：`en.json` 加 `"dashboard.securityScore": "Security score"`、`"dashboard.securityReachable": "reachable exposures"`；`zh.json` 加 `"dashboard.securityScore": "安全评分"`、`"dashboard.securityReachable": "个可达暴露"`。

> `navigate`/`t` 已在 Dashboard 组件内可用（kpis 卡已用）。样式类跟随 Dashboard 现有卡片（实现时对照相邻 KPI 卡的实际 class，保持一致）。

- [ ] **Step 2: 前端全量收口验证**

Run: `cd src/agenticops/web/frontend && npx tsc --noEmit && npm run build`
Expected: 0 errors，build 成功

- [ ] **Step 3: 提交**

```bash
git add src/agenticops/web/frontend/src/pages/Dashboard.tsx \
        src/agenticops/web/frontend/src/locales/en.json src/agenticops/web/frontend/src/locales/zh.json
git commit --no-verify -m "feat(security): Dashboard security highlight card (Stage 6)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

> **阶段 6 验证锚点：** 前端可视 + 报告可生成 + tsc/build 全绿。

---

## Stage 7 — 全量回归 + 真实账户 E2E + 文档

### Task 7.1: 全量回归

- [ ] **Step 1: 清孤儿进程 + 全量 pytest**

```bash
pkill -9 -f "pytest tests" 2>/dev/null; sleep 1
source .venv/bin/activate
python -m pytest tests/ --ignore=tests/integration -o addopts="" -q
```
Expected: 全部通过（对照基线：Stage 0-3 收口时 57 个安全测试全绿 + 全库无新增失败；已知 pre-existing fail 见记忆，非本计划引入的失败不算回归）

- [ ] **Step 2: 语法/构建终检**

```bash
python3 -m py_compile src/agenticops/models.py src/agenticops/security/*.py \
  src/agenticops/services/security_service.py src/agenticops/web/routers/security.py \
  src/agenticops/web/app.py src/agenticops/report/generator.py
cd src/agenticops/web/frontend && npx tsc --noEmit && npm run build
```
Expected: 全绿

- [ ] **Step 3: 有失败则修复后重跑；全绿后提交（若有修复）**

---

### Task 7.2: 真实账户 E2E（只读采集 + 本地验证）★ 含 STOP 门

> 采集全部只读（describe/list/get + credential report）。E2E 不执行任何变更类 AWS 操作。

- [ ] **Step 1: 慢频 E2E**

```bash
source .venv/bin/activate
python -c "
from agenticops.security.posture_snapshot import run_posture_snapshot
print('snapshots written:', run_posture_snapshot())
"
```
Expected: ≥1；随后查库确认 `overall_score`/`cis_results`/`exposure_paths` 非空合理，重跑一次分数一致（可复现）。

- [ ] **Step 2: 快频 E2E**

```bash
python -c "
from agenticops.security.incremental_poll import run_incremental_poll
print('signals emitted:', run_incremental_poll())
"
```
Expected: 正常返回（数量取决于真实 findings）；`security_poll_cursors` 表有行；重跑第二次 emit 数应因指纹去重不再重复建 issue（merged/noise）。

- [ ] **Step 3: Web E2E**

```bash
uvicorn agenticops.web.app:app --port 8000 &
sleep 5
curl -s localhost:8000/api/security/summary | head -c 400
curl -s localhost:8000/api/security/attack-paths | head -c 400
curl -s -X POST localhost:8000/api/reports/generate -H 'Content-Type: application/json' \
  -d '{"report_type": "security-review"}' | head -c 400
```
Expected: summary 返回账户评分；报告 201。浏览器（或 Playwright）验证 `/app/security` 页与 Dashboard 高亮卡渲染。

- [ ] **Step 4: 写 E2E 证据报告**

新建 `docs/MVP-2.5.0-E2E-REPORT.md`：环境、每步命令与实际输出（快照分数、issue 数、去重表现、页面截图）、结论。

- [ ] **Step 5: 🛑 STOP — 向主人汇报 E2E 结果，等确认后才允许 push**

汇报内容：全量回归结果、E2E 证据摘要、建议的 push 范围（`git push --no-verify origin MVP-2.5.0`）。**未获确认前绝不 push。**

---

### Task 7.3: 文档更新

- [ ] **Step 1: CLAUDE.md**
  - Key Modules 表加一行：`security/` | `collectors.py, scoring.py, posture_snapshot.py, incremental_poll.py, reachability.py, advisor.py` | 云安全审查引擎（双频确定性采集 + CIS 可复现评分 + 含 NACL 三态可达性 + 证据接地建议器）
  - Key Configuration 表加 8 个 `security_*` 行（默认值照 settings.yaml）。
  - Project Overview 的 docs 引用加 `docs/MVP-2.5.0-RELEASE.md`。
- [ ] **Step 2: docs/WORKFLOW.md** — 加 Security Review 一节（双频数据流 Mermaid + Security 页/报告用法）。
- [ ] **Step 3: docs/MVP-2.5.0-RELEASE.md** — 标记 P1 阶段 0-7 交付状态 + E2E 报告链接。
- [ ] **Step 4: 提交**

```bash
git add CLAUDE.md docs/WORKFLOW.md docs/MVP-2.5.0-RELEASE.md docs/MVP-2.5.0-E2E-REPORT.md
git commit --no-verify -m "docs(security): MVP-2.5.0 P1 delivery — CLAUDE/WORKFLOW/release/E2E evidence

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec 覆盖（§9 阶段 4-7）：**
- 阶段 4（快频增量 + signal_gate + Dashboard 高亮）→ Task 4.1（cursor）、4.2-4.4（三源 poller）、4.5（编排+gate）、4.6-4.8（慢频 exposure 接线 + 可执行发现入 gate）；Dashboard 高亮按范围决策 4 移至 Task 6.6 ✓
- 阶段 5（建议器 + critic）→ Task 5.1-5.3（spec §3.5：证据接地 + critic + fail-closed + 复用 `_call_bedrock`）✓
- 阶段 6（Security 页 + API + 报告）→ Task 6.1-6.6（spec §3.7 五端点/页面/导航/Dashboard 卡；§3.8 六处编辑）✓
- 阶段 7（E2E + 文档）→ Task 7.1-7.3（spec §8 验收 1-8 条：可达性对抗测试已在 Stage 2-3 绿；fail-soft+cursor 由 test_security_poll 锁；评分可复现由既有 test_security_scoring 锁 + E2E 重跑比对；建议门 test_security_advisor；gate 去重 E2E Step 2；多账户不串号走 provider 层 + Task 4.6；tsc/build/py_compile Task 7.1；E2E+主人确认才 push Task 7.2 STOP 门）✓
- spec §6 失败模式全部有对应测试（源失败 cursor 不前移 / 缺数据 undetermined（Stage 2-3 已锁）/ 不 grounded/refuted 丢弃 / 账户失败隔离（既有）/ LLM 异常不影响快照 / scheduler 失败块（Stage 0 已落地））✓
- spec §7 测试文件：`test_security_poll.py`、`test_security_advisor.py`、`test_security_api.py` 本计划新建；`test_security_reachability/scoring/snapshot/scheduler` Stage 0-3 已有，本计划扩展 snapshot ✓

**2. 占位符扫描：** 无 TBD/TODO/"implement later"；每个代码步骤给出完整实现。两处"以现状为准"注记（Task 6.5 Spinner/Card import 路径、Task 6.3 generator 的 session 入口）是防 drift 的对照指引，均给出了默认实现与调整方向，非缺口。

**3. 类型一致性：**
- `SecurityEvent(source, event_id, title, description, severity, resource_id, resource_type, occurred_at, raw)` — 4.2 定义，4.3/4.4/4.5 一致 ✓
- `_get_cursor(session, account, source, region) -> str` / `_set_cursor(..., value)` — 4.1 定义，4.5 一致 ✓
- `_aggregate_topology(account) -> (instances, subnets, sgs, nacls)` — 4.7 定义，4.8 消费 ✓
- `annotate(findings, instances, subnets, security_groups, nacls=None)` — Stage 2-4 一致（既有签名未改）✓
- `recommend(snapshot_id, account, score_result, findings) -> int` — 5.2 定义，5.3 调用一致 ✓
- service 五函数名与 router/hooks/types 的路径、字段逐一对应（summary/trend/findings/recommendations/attack-paths）✓
- `SecurityPollCursor(account_id, source, region, cursor)` — 4.1 定义，4.5 与测试一致 ✓

**执行者注意的三个校验点（实现前先 grep 确认，不阻塞）：**
- Task 4.1：`models.py` 顶部 import 是否已含 `UniqueConstraint`（AgentMemoryFact 在用，应已有；缺则补）。
- Task 6.3：`report/generator.py` 的 DB session 获取模式（`get_db_session` 是否已 import；`_save_report` 内部自会建 Report 行）。
- Task 6.5/6.6：`Spinner`/`Card` 的实际导出路径与 Dashboard 卡片 class，以相邻代码为准微调。
