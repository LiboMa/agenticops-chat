# eksctl cluster configuration for AgenticOps lab environment
# Usage: eksctl create cluster -f cluster.yaml
#
# Security: All nodes in private subnets (no public IPs).
# Access: kubectl via bastion SSH or SSM. Node access via SSM only.
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: agenticops-lab
  region: ap-southeast-1
  version: "1.30"

iam:
  withOIDC: true

addons:
  - name: vpc-cni
    version: latest
  - name: coredns
    version: latest
  - name: kube-proxy
    version: latest
  - name: aws-ebs-csi-driver
    version: latest
    wellKnownPolicies:
      ebsCSIController: true

managedNodeGroups:
  - name: workload
    instanceType: m5.large
    desiredCapacity: 3
    minSize: 2
    maxSize: 5
    volumeSize: 80
    volumeType: gp3
    privateNetworking: true  # Nodes in private subnets — no public IPs
    ssh:
      allow: true
      publicKeyName: sa-malibo  # SSH only reachable via SSM or bastion in same VPC
    labels:
      role: workload
    iam:
      attachPolicyARNs:
        - arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy
        - arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy
        - arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
        - arn:aws:iam::aws:policy/CloudWatchAgentServerPolicy
    tags:
      environment: lab
      project: agenticops

  - name: monitoring
    instanceType: m5.large
    desiredCapacity: 2
    minSize: 2
    maxSize: 3
    volumeSize: 100
    volumeType: gp3
    privateNetworking: true  # Nodes in private subnets — no public IPs
    labels:
      role: monitoring
    taints:
      - key: dedicated
        value: monitoring
        effect: NoSchedule
    iam:
      attachPolicyARNs:
        - arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy
        - arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy
        - arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
    tags:
      environment: lab
      project: agenticops

cloudWatch:
  clusterLogging:
    enableTypes:
      - api
      - audit
      - authenticator
      - controllerManager
      - scheduler
